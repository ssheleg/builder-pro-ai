<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Hardened runtime for a WKWebView app that also embeds signed sidecars (spec §14.3): the app
       uses JS, so it needs the JIT / unsigned-executable-memory / library-validation exceptions
       WKWebView + the deep-signed daemon sidecars require, plus allow-dyld-environment-variables
       for the sidecar launch. BL-174 TRIED to drop the latter two (dyld-injection surface) but the
       narrowed set broke the deep-sign / notarization (sign-verify failed pre-publish), so all four
       are restored — the narrowing needs a deeper rework (re-sign the sidecars separately, or a
       Tauri config) and stays open. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
