# UX Audit — 2026-07-22

- **Scope:** all (SCN-001..SCN-044)
- **Method:** static code trace — 8 parallel batch subagents, each verifying every scenario AND its newly-traced story's Given/When/Then acceptance criteria against current code, evidence-cited.
- **Base version:** working tree on `nightbuild` atop `4337655`, with the new `foundation.md` and the ux-contract-v2 migration of `scenarios.md` staged this session (first audit run WITH the WHY layer / `Traces` chain).
- **Trigger:** 44 files under `src/` moved since the last 44/44 PASS (`df97dcc`) via the "Soft Control Room" design-system v2 migration — verdicts needed re-verification against code that shifted underneath them.

## Summary

- **Totals: PASS 44 / PARTIAL 0 / FAIL 0 / BLOCKED 0.**
- **No functional regressions.** The design-v2 migration is confirmed **visual-only** (fills/tokens/radii/inset-shadow tone edges): every scenario's entry point is reachable, every listed UI element exists and is wired, every state branch (loading/empty/error/success) renders, every listed error is surfaced honestly (no silent catch, no fake success) with its described recovery, and every traced story's acceptance criteria are observable in current code.
- **Chain confirmed serving jobs, not just rendering elements.** Each scenario was checked against its ST acceptance criteria — the job outcome is reachable, not merely the buttons.
- **Top issues:** none user-facing. The only findings are **7 minor/trivial Coverage-cite drifts** in `scenarios.md` (documentation, not code) — line numbers shifted by the migration. Three (SCN-025, SCN-029, SCN-044) overrun end-of-file by 1–2 lines and were missed by the 2026-07-21 cite refresh. **All 7 remediated in this same change** (see Findings register → Remediation).
- **Recommended next actions:** 1. (done) correct the 7 drifted cites; 2. add a lightweight CI guard so a Coverage cite pointing past EOF fails the build (prevents silent re-drift on the next refactor); 3. proceed to the coverage/chain audit (orphan atoms surfaced separately).

## Batch 1: onboarding, capture, home (SCN-001..005)

- **SCN-001 — PASS** — store.ts:470 (`view:"home"` default), HomeView.tsx:239-268 (zeroed tiles + "No active sessions." with button only when a workspace exists), WorkspaceSidebar.tsx:272-284,426-462, App.tsx:60,335-388 (backoff `[500,1000,2000,5000]`), DaemonBanner.tsx:61-77 (red role=alert). AC met.
- **SCN-002 — PASS** — WorkspaceSidebar.tsx:115-127 (pick→create→navigate; cancel no-op; catch→`describeCommandError` covering disconnected/incompatible/tooLarge/internal), :25-43,445-461. AC met.
- **SCN-003 — PASS** — QuickCapture.tsx:24-29 (`isTypingTarget`), :143-183 (⌘K listener + upgrade-dialog guard + submit), :191 (`blocked`), :249-273 (orchd-down note); App.tsx:491. AC met. *(cite drift → AUD-2026-07-22-04)*
- **SCN-004 — PASS** — HomeView.tsx:204-211 (`goTo`: activate ws→view→session→focus), :218-268 (tiles + tone + empty), :271-378 (needs-you / running / finished rows). AC met. *(cite drift → AUD-2026-07-22-05)*
- **SCN-005 — PASS** — HomeGoals.tsx:140-200 (per-project refresh, strategic title + child chips, gated "Goals are loading…", empty→null), store.ts:696-703,760. AC met.

## Batch 2: chrome, projects (SCN-006..012)

- **SCN-006 — PASS** — ThemeToggle.tsx:8-26 (cycle system→light→dark, icons ◐/☀/☾), theme.ts:9-81 (`bpa-theme`, apply-before-paint, OS listener gated to "system"), main.tsx:12 (`initTheme()` pre-render). AC met. *(cite drift → AUD-2026-07-22-06)*
- **SCN-007 — PASS** — WorkspaceSidebar.tsx:189,212,235,296,399,110-113,371-387 (all nav destinations + archived collapse ▸/▾), store.ts:573,760. AC met.
- **SCN-008 — PASS** — WorkspaceSidebar.tsx:107-139,322-362 («No project» group inline link select → `orchdAddProjectWorkspace`→refresh; reject→`describeOrchdError`+reset). AC met.
- **SCN-009 — PASS** — CreateProjectDialog.tsx:228-372 (name focus/Escape, blocked alert + disabled Create at 0 ws / empty name, inline "+ create workspace", double-submit guard), App.tsx:200. AC met.
- **SCN-010 — PASS** — ProjectPanel.tsx:141-165,283-415 (7 tabs, eager counters, unlink/attach→refresh, "workspace unavailable" badge, OrchdDownBanner + gated mutations). AC met.
- **SCN-011 — PASS** — ProjectPanel.tsx:198-242,417-475 (copy JSON / save / import; empty-folder honest note; catch→`describeOrchdError`). AC met.
- **SCN-012 — PASS** — ProjectPanel.tsx:248-341,477-490 (archive confirm→read-only+Archived(N), export stays live, un-archive no confirm, orchd-down disables both). AC met.

## Batch 3: terminals (SCN-013..019, 044)

- **SCN-013 — PASS** — TerminalTabs.tsx:57-81,166-181 (root-aware cwd, disabled at no-workspace), App.tsx:133-137,542-546. AC met.
- **SCN-014 — PASS** — TerminalTabs.tsx:109-137, TerminalPane.tsx:51-67 (hide not dispose), terminal-manager.ts:413-419,523-544,584-589 (hidden buffering, no re-spawn). AC met.
- **SCN-015 — PASS** — TerminalTabs.tsx:83-97 (`finally` dispose+remove = no zombie even if kill rejects), terminal-manager.ts:627-641, store.ts:505-513. AC met.
- **SCN-016 — PASS** — StatusDot.tsx:13-57 (exited-wins branch first), theme.ts:84-101 (tones), store.ts:515-555 (setLifecycle early-return on exited). AC met.
- **SCN-017 — PASS** — CommandStrip.tsx:44-71 (✓/✗/running/interrupted), :176-201 (failed→Retry + toast, distinct loading/empty). AC met.
- **SCN-018 — PASS** — terminal-manager.ts:242-312 (link handler: http(s)→browser, file://→root-match→preview or outside-root toast, other schemes ignored), link-provider.ts:127-176. AC met. *(cite precision → AUD-2026-07-22-07)*
- **SCN-019 — PASS** — App.tsx:162-183,335-345 (reconnect resets attachments then re-attaches visible; cold-boot hydrate+auto-activate), terminal-manager.ts:372-374,509-520 (`term.reset()` before replay = no dup scrollback). AC met.
- **SCN-044 — PASS** — terminal-manager.ts:51-69 (`describeAttachError`), :460-483 (records mapped failure + notify under stale generation), :427-430 (cleared when fresh attach starts), :627-641 (dispose drops it, notify at :640), :643-661 (subscribe API), TerminalPane.tsx:47-121 (role=alert overlay + Retry). AC met. *(cite drift → AUD-2026-07-22-01)*

## Batch 4: files (SCN-020..024)

- **SCN-020 — PASS** — FileTree.tsx:111-116,170,333-362,658-687,721-804 (dirs-first sort, show-ignored filter, lazy fetch+cache, failed→inline Retry no auto-loop, +Add root), FilesRail.tsx:32-142, store.ts:590-607 (`invalidateDirs(root,["*"])` keeps expanded). AC met.
- **SCN-021 — PASS** — FilePreview.tsx:60-145 (token-guarded refetch, binary·size, tooLarge·size, truncation banner, error card+toast), fs.ts:30-45, fs_explorer.rs:37,301-359. AC met.
- **SCN-022 — PASS** — FileTree.tsx:536-654,728-755,421-448 (row menu, inline form autofocus, Enter/Escape, re-list parent), fs_explorer.rs:366-399 (no-clobber create, AlreadyExists rename). AC met.
- **SCN-023 — PASS** — FileTree.tsx:450-463,575-588 (confirm→trash→re-list, clears preview if deleted was selected), fs_explorer.rs:426-449 (OS Trash, never remove_*). AC met.
- **SCN-024 — PASS** — App.tsx:160,445-462 (watch-error→pause; success clears stale, catch re-pauses), FilesRail.tsx:66-114,178-198 (collapsed warn dot, refresh drops all cached dirs, never-falsely-live). AC met.

## Batch 5: ideas, research, insights (SCN-025..029)

- **SCN-025 — PASS** — IdeasList.tsx:21-28,154-314,333-492 (dashed create form, inline commit+silent revert, six-value lifecycle, "delete idea?" confirm, newest-first, orchd-down disables mutations/reads live). AC met. *(cite drift → AUD-2026-07-22-02)*
- **SCN-028 — PASS** — WorkspaceSidebar.tsx:230-269 (✉ Inbox + orphan badge), App.tsx:511-513, InboxPanel.tsx, SpawnProjectFromIdea.tsx:62-124 (resumable via createdWorkspaceId/createdProjectId, "Retry linking", no duplicates). AC met.
- **SCN-026 — PASS** — ResearchRunDialog.tsx:106-306 (connected-only servers, seeded args, spend preflight, invalid-JSON inline, start-reject inline+toast keeps open), ResearchPane.tsx:110-218 (2s self-poll to terminal, show-artifact, failed-run errorKind still offers insight). AC met.
- **SCN-027 — PASS** — FormInsightDialog.tsx:213-336,409-457 (fit-context goals+graph gated on project, three-stage Create→Accept→To-backlog, orphan "context unavailable" blocks backlog, createdInsightId/createdTaskId resume guards → no double-create). AC met.
- **SCN-029 — PASS** — InsightsList.tsx:33,117-281,339-363 (status select, archive requires typed reason, verdict override, fit badge fit/no-fit/unclear/—, orchd-down disables, empty note). AC met. *(cite drift → AUD-2026-07-22-03)*

## Batch 6: tasks, goals, graph, rules (SCN-030..032, 036)

- **SCN-030 — PASS** — TasksList.tsx:336-366,461-482 (six groups always with counts, single fractional-rank move, confirm names cascade via countDescendants, ▲/▼ edge-disabled), ProjectPanel.tsx:496. AC met.
- **SCN-031 — PASS** — GoalTree.tsx:218-330,432-536 (seeded subgoal, inline rename revert, ▲/▼ ord-swap, "delete the entire branch?" confirm, metric chips, strategic root non-movable/non-deletable, no top-level add, empty note), HomeGoals.tsx:164-200. AC met.
- **SCN-032 — PASS** — GraphCanvas.tsx:59-71,394-591,656-758 (add-node kinds, 400ms debounced move, optimistic edge + rollback+toast, dbl-click rename, edge-kind select, "delete selection?" + refresh reconcile, stale-search drop, entityRef "ref·type"/"source removed", external ghost opens project, fresh-orchd-down early return), graphMapping.ts:89-109. AC met.
- **SCN-036 — PASS** — RulesetPanel.tsx:36-57,321-514 (rules textarea+Save, policy form spend-cap/classes/paths, inline validation, externally-modified→Accept / lost→Recreate info banners, reveal-file stays live under orchd-down), ProjectPanel.tsx:35,498. AC met.

## Batch 7: extensions (SCN-033..035)

- **SCN-033 — PASS** — ServersTab.tsx:189-297 (add form, OAuth/stdio/project "soon" disabled, connect only sets target), ConnectDialog.tsx:105-152 (`mcpConnect` runs ONLY after `trustGrantConsent(...,"connect")`; endpoint+access note; error inline+toast keeps open), ExtPanel.tsx:88, strings.ts:45,497-501. AC met.
- **SCN-034 — PASS** — ToolsBrowser.tsx:138-267 (allowlist toggle, schema, JSON-arg invoke, **unconditional** "⚠ unverified data" badge, "the tool returned an error"), ConnectorsTab.tsx:506-657 (API-key + OAuth start→page→paste→finish, unverified banner, honest no-providers note), consent recovery via describeWithRecovery/isConsentError. AC met.
- **SCN-035 — PASS** — InvocationLog.tsx:124-297 (limits persist via trustSetPolicy+refresh, number validation, Calls + Audit tables, honest empties), ArtifactsTab.tsx:96-172 (show/hide + unverified badge), SkillsTab.tsx:100-303 (add/delete, modified/file-missing badges, registry plumbing note). AC met.

## Batch 8: system-status, diagnostics, error-recovery (SCN-037..043)

- **SCN-037 — PASS** — DaemonBanner.tsx:61-77 (red self-healing role=alert no-dismiss), App.tsx:161-183,475. AC met.
- **SCN-038 — PASS** — UpgradeDialog.tsx:140-219 (sessiond precedence, hydrated-gated count copy, retry-safe, inline launchctl failure hint), DaemonBanner.tsx:25-59 (amber re-entry + Update), App.tsx:184-193,365-378 (poll fallback for lost single-shot event). AC met.
- **SCN-039 — PASS** — OrchdDownBanner.tsx:43-56 (red + Retry→orchdReconnect), App.tsx:249-304,487 (global + per-tab-bar mounts; onOrchdUp rehydrates every live slice + storage), mutating controls gated across ideas/insights/tasks/goals/graph/rules/extensions; reads live; QuickCapture inline note. AC met.
- **SCN-040 — PASS** — UpgradeDialog.tsx:141-262 (orchd eligible only when sessiond dialog absent, inline failure), OrchdUpgradeBanner.tsx:48-61 (persistent re-entry, no dead-end), App.tsx:311-317. AC met.
- **SCN-041 — PASS** — StorageBanner.tsx:32-45 (red role=alert only for in_memory_fallback/recovered_from_corruption, no dismiss), App.tsx:302,402,478, strings.ts:617-619 (blunt "will NOT survive a restart" / damaged-copy path). AC met.
- **SCN-042 — PASS** — DiagnosticsPanel.tsx:18-85 (copy bundle, Clear, empty state, rows), diag.ts:28,62-103 (DIAG_CAP=200, scrubbers, newest-first, toSupportBundle), WorkspaceSidebar.tsx:86,462-503, store.ts:642-665. AC met.
- **SCN-043 — PASS** — ErrorBoundary.tsx:23-87 ("Something broke" card, `{name}: {message}`, Reload primary + Copy details), main.tsx:19-21, store.ts:667-678 (scrubbed, 6-line stack cap). AC met.

## Findings register

| # | Scenario | Severity | Finding | Suggested fix | Status |
|---|----------|----------|---------|---------------|--------|
| AUD-2026-07-22-01 | SCN-044 | minor | `terminal-manager.ts:644-668` overruns EOF (666 lines) by 2; the "dispose drops it" logic the scenario names is at 627-641, uncited | retarget → `:51-69,133,427-431,460-483,627-641,643-661` | **remediated** |
| AUD-2026-07-22-02 | SCN-025 | minor | `IdeasList.tsx:334-493` overruns EOF (492); `155-315` off-by-one vs IdeaRow at 154-314 | retarget → `:21-28,154-314,333-492` | **remediated** |
| AUD-2026-07-22-03 | SCN-029 | minor | `InsightsList.tsx:340-364` overruns EOF (363 lines) | retarget → `:33,117-281,339-363` | **remediated** |
| AUD-2026-07-22-04 | SCN-003 | minor | `QuickCapture.tsx:144-184,192,250-273` shifted one line up after migration | retarget → `:24-29,143-183,191,249-273` | **remediated** |
| AUD-2026-07-22-05 | SCN-004 | minor | `HomeView.tsx:205-212` (`goTo`) off-by-one | retarget → `:204-211,240-378` | **remediated** |
| AUD-2026-07-22-06 | SCN-006 | trivial | `main.tsx:10` is a comment line; the `initTheme()` call is at :12 | retarget → `main.tsx:8,12` | **remediated** |
| AUD-2026-07-22-07 | SCN-018 | trivial | `link-provider.ts` cite omits the file:// containment check `matchWorkspaceRoot` at :127-139 | add `:127-139` | **remediated** |

All findings are documentation-only cite drift consistent with the design-v2 line shifts; **none change a verdict** and none indicate a functional defect. Every scenario-quoted UI string was re-confirmed against `src/strings.ts` during the trace.

## Definition of done

- Every scoped scenario (SCN-001..044) has a PASS verdict with `file:line` evidence and story-AC confirmation — none skipped, none a courtesy pass.
- Report on disk; base `Last audit` updated to `2026-07-22 PASS` for all 44.
- 7 cite-drift findings surfaced AND remediated in the same change; a CI EOF-guard recommended to planning.
