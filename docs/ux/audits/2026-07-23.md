# UX Audit — 2026-07-23

- **Scope:** SCN-045..SCN-057 (all scenarios never audited before: supervisor, power, tasks-priority, analytics, docs, home-v2, onboarding fast-path, auth)
- **Depth:** deep
- **Method:** static code trace; passes: scenario, flow conformance, heuristics, practices, coverage
- **Base version:** 9b11c36 (docs/ux at audit time, branch nightbuild)

## Re-audit (post-remediation, same day)

The three PARTIAL scenarios were fixed and re-audited (static trace) — **all three now PASS**, every finding closed with file:line evidence, zero residual:
- **SCN-056 → PASS** — AUD-17 closed: fast-path spawns with `cwd: ws.roots[0] ?? dir` (WorkspaceSidebar.tsx:144); cold-start test asserts the cwd (WorkspaceSidebar.test.tsx:193); TerminalTabs comment corrected.
- **SCN-052 → PASS** — AUD-07/08/09/10/25 closed: epoch guard (store.ts:1125,1131) + cancelStats; FamilyUsage per-model cut (stats.rs:60-73,398-455 → StatsView "By model family" panel); empty-only-when-both-empty; "—" tiles on fail/loading; precondition de-scoped; +7 tests.
- **SCN-053 → PASS** — AUD-11/12/13/14 closed: Cancel button; stats_git honest worker-failure (stats.rs:588-614, no unwrap_or_default); lastRefreshMs stamp fallback; gitReason title.

Remediation commits: 621c8d9 (fast-path), 1f3a0ad (stats), 7f30870 (polish). Gate: cargo -p builder-pro-ai 268/1 (the 1 = non-hermetic `connect_orchd_with_retry_gives_up`, fails only because the installed app's live orchd socket is present — unrelated to this scope), vitest 1070/1070, tsc 0, vite build ok, lint green. Final board: **PASS 7 / PARTIAL 0 / FAIL 0 / BLOCKED 6**.

---

## Summary (initial pass)

- **Totals: PASS 4 / PARTIAL 3 / FAIL 0 / BLOCKED 6** (13 scoped)
  - PASS: SCN-045 (keep-awake), SCN-046 (CEO config, within its documented S6b honesty boundary), SCN-051 (task priority), SCN-054 (project docs)
  - PARTIAL: SCN-052 (usage stats), SCN-053 (output stats), SCN-056 (first-run fast-path)
  - BLOCKED — not built, claims verified honest: SCN-047/048/049/050 (CEO runtime, scheduled S6b), SCN-055 (Home v2, awaiting build), SCN-057 (auth context, draft gated on A-9)
- **Top issues (worst user damage first):**
  1. [AUD-2026-07-23-17] (major) SCN-056 — fast-path auto-spawn lands the first terminal in `$HOME`, not the picked workspace root; the very moment the fast-path exists to optimize (P-02's first aha) delivers a wrong-directory shell, and the test locks the bug in.
  2. [AUD-2026-07-23-25] (major) SCN-052 — range-switch race: a stale scan reply can overwrite a newer one, rendering figures under the wrong range label — dishonest data in the one view whose register is honesty.
  3. [AUD-2026-07-23-07] (major) SCN-052 — the promised per-agent cut does not exist anywhere in the chain (scan buckets, wire type, view); ST-038 AC1 not observable.
  4. [AUD-2026-07-23-11] (major) SCN-053 — "slow scan → cancellable" recovery is missing entirely.
  5. Minor honesty polish cluster in stats (zeros-as-data on load/error, fake-empty on git worker failure, stampless git table) + doc-layer drift (coverage cites, flow tags — the latter fixed in this change).
- **Integrity headline:** all six "Coverage: none yet" claims verified honest — no phantom UI, no dead chrome promising unbuilt behavior; SCN-046's S6b pending note renders verbatim; SCN-004 correctly not retired while Home v2 is unbuilt.
- **Recommended next actions (prioritized):**
  1. Fix AUD-17 (one-line cwd + test assert) — highest damage-to-effort ratio in the set.
  2. Fix AUD-25 epoch guard + fold in AUD-11 cancel and the zeros-as-data minors (08/09/12/13) — one coherent StatsView/store change.
  3. Decide AUD-07: build the per-agent dimension or descope "per agent" from SCN-052/ST-038 explicitly.
  4. Refresh coverage cites (01/04/05/06/16) — mechanical, restores audit re-runnability.
  5. Proceed to the A-9 spike (checks formalized in foundation) to unblock ST-043/SCN-057; then build SCN-055 (Home v2) whose flow (FLW-03) is verified free of contradicting partial code.

## Batch 1: power + supervisor config (SCN-045, SCN-046)

### SCN-045 — PASS
- **Context:** ST-033 acceptance criteria: all three met.
  - AC1 (enabled + ≥1 live session → sleep prevented + indicator): reconcile rule `want = enabled && live_sessions > 0` acquires the IOPM assertion — src-tauri/src/power.rs:74-88 (pure rule), :286-328 (`IOPMAssertionCreateWithName`, `PreventUserIdleSystemSleep` :253); live count from non-exited sessions src/App.tsx:462-476; indicator green dot + "keep-awake · on" only when genuinely held src/components/WorkspaceSidebar.tsx:586-591, src/strings.ts:700.
  - AC2 (last session ends / toggle off → release): power.rs:81-83; store setKeepAwakeEnabled src/store/store.ts:1056-1070 → power.rs:363-369; tests `last_session_ending_releases` power.rs:529-539, `toggle_off_mid_hold_releases` :541-556. Quit/crash: process-scoped assertion auto-released by kernel (power.rs:25-28,191-196 + Drop :347-356) — no orphan lock.
  - AC3 (OS denial honest): denial propagates with `held == false` power.rs:318-327; `active` is always `is_held()`, never intent :139-145; reportPowerFailure → DiagEvent + toast store.ts:594-609,795-817; copy "keep-awake unavailable: {reason}" strings.ts:707; danger dot WorkspaceSidebar.tsx:581-591; once-per-streak toast gate store.ts:587-609.
- **Evidence:** power.rs:36-45,49-88,95-161,163-187 (honest non-macOS "unsupported" stub),197-356,363-387; src-tauri/src/lib.rs:604,727-729; src/ipc/power.ts:13-48; store.ts:435-457,509-545 (default-ON persistence `bpa-keep-awake`),587-610,1054-1080; App.tsx:443-456,458-476; WorkspaceSidebar.tsx:87-90,470-480,563-636; strings.ts:697-708. Tests: power.rs:452-669; WorkspaceSidebar.test.tsx:343-401; + store.test.ts, App.test.tsx keep-awake coverage.
- **Flow conformance:** FLW-18 fully implemented — every node/edge mapped (S→A power.rs:75; A-no→normal, test :460-478; H-yes→indicator; H-no→failure+Diagnostics store.ts:594-609; L→REL power.rs:81-83 + App.tsx:474-476). **Stale `*(to-be)*` tag at flows.md:396.**
- **Findings:**
  - [AUD-2026-07-23-01] (minor) Coverage-cite drift in SCN-045 Coverage (scenarios.md:865): lib.rs:719-721→727-729; strings.ts:615-627→697-708; store.ts:408-431/452-477/964-990→435-457/509-545+587-610/1054-1080; App.tsx:413-446→443-476; WorkspaceSidebar.tsx:447-458/540-613→470-480/563-636 -> refresh cites.
  - [AUD-2026-07-23-02] (minor) FLW-18 tagged `*(to-be)*` (flows.md:396) while fully implemented -> drop tag.

### SCN-046 — PASS
- **Context:** ST-034 acceptance criteria, read within the documented S6b honesty boundary — runtime ACs (answer/park/log) **correctly absent**; their config prerequisites observable: enable default-false orchd-proto/src/lib.rs:248-254, delegated classes :255-258, persisted in PolicyRules :278-284, empty-scope-with-CEO-on rejected client RulesetPanel.tsx:67-69 AND server persistence.rs:2634-2639. Honesty note renders verbatim: strings.ts:323 → RulesetPanel.tsx:761-763 (`role="note"`, testid `ruleset-supervisor-pending`), asserted in RulesetPanel.test.tsx:327-330.
- **Evidence:** Entry: ProjectPanel Rules tab → RulesetPanel scope="project" ProjectPanel.tsx:28,36,500; supervisor section project-only RulesetPanel.tsx:664 (negative test :337-341). Steps: toggle :668-681; class checkboxes :683-704 + caps summary :706-708; instruction :722-735; custom rules ChipList :737-749; "Recommended scope" preset :31,514-521,710-720; save :773-783 → validatePolicy :46-71 → daemon validate persistence.rs:2592-2641. Persistence: SupervisorConfig additive `#[serde(default)]` orchd-proto:225-284; TS orchd-types.ts:209,300-323; decode backfill persistence.rs:2670-2682. All 10 listed UI elements present incl. "MCP tools — soon" placeholder :757-759 (strings.ts:348). Errors: inline `role="alert"` :767-771 + toast :509-511; orchd-down disables all controls :674,697,715,728,743,778 (test :408-424); blocked alert strings.ts:350 (tests :381-406) + backend guard (test persistence.rs:6238-6268). Tests: persistence.rs:6176-6324+, roundtrip.rs:1467-1517, ts_export.rs:159-200, RulesetPanel.test.tsx:301-436 (7 tests).
- **Flow conformance:** FLW-19 config nodes (SET→ON) implemented; runtime nodes unimplemented — `*(to-be — the autonomy core)*` tag NOT stale (S6b pending), correct.
- **Findings:**
  - [AUD-2026-07-23-03] (minor) Step 2 promises "effective caps (spend, calls/min)" but the summary shows spend only (RulesetPanel.tsx:706-708; comment :534-535 says per-project policy carries no calls/min) while a project-scoped trust Policy `ratePerMin` exists (orchd-types.ts:196) and is never surfaced -> surface trust-policy ratePerMin in the caps line, or amend SCN-046 step 2 (scenarios.md:877).
  - [AUD-2026-07-23-04] (minor) Coverage-cite boundary nits (scenarios.md:886): orchd-types 300-305→300-323; persistence 2607-2634→..2639; RulesetPanel 455-536→..545; tests 301-430→301-436 -> widen cites.

## Batch 2: tasks + analytics (SCN-051, SCN-052, SCN-053)

### SCN-051 — PASS
- **Context:** ST-037 AC1 (persist; urgent distinct; sorts ahead in status group) met: persistence.rs:2443-2467 (set_task_priority) on v5 column :649-655 (CHECK + default normal); danger marker TasksList.tsx:188-199,261-266 + chip :270-274; urgent-first sort :105-109. AC2 (continuation consumes urgent first) — S6b future, daemon ready (list_tasks returns priority, orchd-proto:1369-1372).
- **Evidence:** create-form select TasksList.tsx:532-544, per-row :280-293; create path :472-482 → orchd.ts:247-269 → commands.rs:1627-1655 (lib.rs:648); row path :406-412 → orchd.ts:290-292 → commands.rs:1708-1720 (lib.rs:652) → SetTaskPriority orchd-proto:1369-1372 (map :1516) → socket_server.rs:1257-1263 (TasksChanged) → persistence.rs:2443-2467; refresh events.ts:158-161. TaskPriority enum orchd-proto:206-213; TS orchd-types.ts:75,332. Reject→toast :409-411, revert implicit (controlled by store :283, doc :402-405); orchdDown gating :284,298,312,322,333,572,606. Tests: TasksList.test.tsx:313-321,345,380,411,428,440,457+; persistence.rs:3506,5469,5514,5557,5566; dispatch_integration.rs:1287-1405; roundtrip.rs:717,1005.
- **Flow conformance:** FLW-13 verified (create w/ priority, create-fail→toast, urgent marker + sort, status/rank/delete nodes). "CEO consumes first" edge = S6b.
- **Findings:**
  - [AUD-2026-07-23-05] (minor) Coverage-cite drift: orchd-proto cite 1258-1265 now holds Connector verbs; SetTaskPriority actually :1369-1372 (map :1516) -> update cite.
  - [AUD-2026-07-23-06] (minor) Cite drift: commands.rs:1611-1710 truncates orchd_set_task_priority (actual 1708-1720); persistence cites 631-651→642-655 (migrate_v5), 2406-2435→2438-2467 -> refresh ranges.

### SCN-052 — PARTIAL
- **Context:** ST-038 AC1 partial — per project YES (StatsView.tsx:200-212 tiles, :244-266 rows; range end-to-end store.ts:1086-1089 → stats.ts:50-52 → stats.rs:227-235,322-339); **per agent NO** (finding 07). Honest empty state YES (StatsView.tsx:161,196-197; test :155). ST-040/COV-01 closure observable: SegmentedPill + Heatmap primitives (primitives.tsx:437,522) consumed at StatsView.tsx:170,219.
- **Evidence:** nav WorkspaceSidebar.tsx:292-313 → App.tsx:581-583; first-open fetch StatsView.tsx:137-140; loading :194; pill :170-180 (refetch test :118); per-project table :228-266; Heatmap :219-224; cwd→project longest-prefix + honest "other" :22-40 (tests :86-98); per-source degradation Promise.allSettled store.ts:1099-1121, notes role="alert" StatsView.tsx:183-192 (strings.ts:740-741); whole-scan failure in payload stats.rs:464-493; Retry=Refresh :240-242; toast store.ts:1123-1125→815. Tests: StatsView.test.tsx:109,118,136,146,155,163; scanner stats.rs:544-718.
- **Flow conformance:** FLW-20 core nodes verified. Gaps: "Home spend tile" entry point does not exist (HomeView.tsx:239-255 — workspaces/live/waiting only); `*(to-be)*` tag stale for the shipped part.
- **Findings:**
  - [AUD-2026-07-23-07] (major) Per-agent cut absent across the chain: DayUsage buckets day×cwd only (stats.rs:38-54; family folded after pricing :321-374), wire type mirrors (stats.ts:10-25), view renders per-project only. Scenario element "per-agent stat tiles" + ST-038 AC1 not observable -> add agent/model-family dimension to buckets + per-agent cut in view, or descope "per agent" explicitly from SCN-052/ST-038.
  - [AUD-2026-07-23-08] (minor) Empty predicate ignores git (StatsView.tsx:161 checks usage days only): usage-empty + git-nonempty range hides real git output behind EmptyState -> empty only when both sources empty.
  - [AUD-2026-07-23-09] (minor) usageError + days=[] renders 0-token/0-session tiles beside the alert — zeros styled as data for a failed source -> dash/suppress usage tiles when usageError set, keep git figures.
  - [AUD-2026-07-23-10] (minor) Precondition names "MCP invocations" as a source; only session JSONL scanned (stats.rs:1-25,260-376), InvocationLog costs not joined -> wire the source or drop it from the precondition.

### SCN-053 — PARTIAL
- **Context:** ST-039 AC1 met: `git log --numstat --since` stats.rs:382-445 (cutoff :389-391, parse :420-436); roots store.ts:1097; cell `{commits} · +added −deleted` StatsView.tsx:258-261; totals :209-210; test stats.rs:739-756. AC2 met: every failure → available:false + reason (stats.rs:392-418; test :758-764); UI "no git data" StatsView.tsx:262-264 via buildRows :114-124; test StatsView.test.tsx:128.
- **Evidence:** statsGit stats.ts:55-57 → stats_git stats.rs:498-509 (spawn_blocking, per-root honesty); shared pill both sources store.ts:1086-1101; "as of {time}" StatsView.tsx:235-239 (semantics stats.rs:60-64); Refresh :240-242; loading :194; failure → gitError store.ts:1116-1120 → note + toast.
- **Flow conformance:** FLW-20 no-git node verified; same stale-tag remark.
- **Findings:**
  - [AUD-2026-07-23-11] (major) "Slow scan → visible loading, cancellable" not implemented: no cancel affordance, no abort path in refreshStats (store.ts:1091-1126) or Rust commands (stats.rs:466-509) -> add cancel (generation-token drop of pending promise; optional cancellation flag through spawn_blocking), or amend SCN-053 to drop "cancellable".
  - [AUD-2026-07-23-12] (minor) stats_git returns `res.unwrap_or_default()` (stats.rs:508): panicked worker yields empty Vec as success — gitError stays null, rows silently vanish; fake-success shape (contrast stats_usage :488-493) -> mirror stats_usage honesty.
  - [AUD-2026-07-23-13] (minor) Freshness stamp sources usage.asOf only (StatsView.tsx:235-239): usage-failed renders git table with no stamp; stamp describes the wrong scan -> stamp from refresh completion (or add asOf to git reply).
  - [AUD-2026-07-23-14] (minor) ProjectRow.gitReason captured (StatsView.tsx:74,121-123) never rendered — honest per-root reason dies in the view -> tooltip/title on the no-git cell or delete the dead field.

## Batch 3: docs + onboarding (SCN-054, SCN-056)

### SCN-054 — PASS
- **Context:** ST-041 acceptance criteria: all three met.
  - AC1 (create/edit → persists with project, renders markdown): orchdUpsertDoc DocsPanel.tsx:308-348 → orchd.ts:374-380 → commands.rs:1830-1846 → persistence.rs:3009-3074 (row + file atomically); preview parseMarkdown DocsPanel.tsx:169-231,550 + markdown.ts:33-118; wire lifecycle dispatch_integration.rs:948-1075 (file at `rules/docs/<project-id>/<name>.md`, :991).
  - AC2 (external change / lost file → Accept/Recreate, never silent divergence): fresh hash-check on every doc-returning reply socket_server.rs:608-614; externallyModified + Accept DocsPanel.tsx:501-516,350-358; missing + Recreate :518-533,360-369; daemon persistence.rs:3089-3110, tests :6734,:6750,:6774,:6823.
  - AC3 (orchd down → mutations disabled, reading live): disabled on +doc :601, Save :562, Delete :493, Accept :509, Recreate :526; textarea/toggle/reveal ungated :478-487,535-551; Enter-create gated :588; test DocsPanel.test.tsx:326-355.
- **Evidence:** Docs = 8th tab ProjectPanel.tsx:28-38,502. List name + relative mtime DocsPanel.tsx:397-445 (:38-44 clamps future mtime; mtime from file socket_server.rs:617-627). Empty-name blocked :601 + name-rule mirror :24-30 + dup guard :317-320. Edit/preview SegmentedPill :468-477 (JSX-only preview, no HTML strings :167-176). Delete + confirm :371-384 (strings.ts:379). Reveal no-path-from-JS commands.rs:1936-1964 (test :4823; UI passes (projectId,name) only — DocsPanel.test.tsx:290-301). Empty state strings.ts:366, DocsPanel.tsx:405-411 (test :90-100). Save-reject inline+toast, draft preserved :341-347 (test :202-222). Backend: schema v6 doc table persistence.rs:56,666-671; validate_doc_name choke-point :2876-2901 (traversal test :6583); 20 doc tests :6495-6843 + migration :3586; archived guards :6542,:6714,:6799. Events broker.rs:97,305; events.ts:340-342; App.tsx:219-230 + reconnect :302-313. Tests: DocsPanel.test.tsx 20 its; markdown.test.ts 11 (:8-82). Note (not a gap): inline markup passes through verbatim — minimal block parser is the documented design (markdown.ts:1-21).
- **Flow conformance:** FLW-21 verified — every edge has a code path. **Stale `*(to-be)*` tag at flows.md:462.**
- **Findings:**
  - [AUD-2026-07-23-15] (minor) FLW-21 stale `*(to-be)*` tag (flows.md:462) for an implemented flow -> drop tag.
  - [AUD-2026-07-23-16] (minor) Coverage-cite drift (scenarios.md:1007): strings.ts:317-347→354-390 (317-347 now CEO strings); orchd-proto 792-849→862-899; verbs 1331-1373→1381-1421; persistence :6241+→:6495+; "19 tests"→20; store.ts:894-925→915-941 (slices :198-206) -> refresh cites.

### SCN-056 — PARTIAL
- **Context:** ST-002 AC1 met (pick folder → workspace, navigate in; WorkspaceSidebar.tsx:134-137; test :146-155; cancel quiet no-op :135). AC2 met (create-fail toast names reason :145-147; test :168-179).
- **Evidence:** fast-path gate = app-wide cold-start only: `hadSessions` snapshot before round-trip :132, spawn gated `!hadSessions` :138 (steady-state test :195-208); auto-spawn `createSession(ws.id)` :140 (cold-start test :183-193); focus via session://created → App.tsx:136-139 (activeSessionId null on cold start); degradation: navigation before spawn :137, inner try/catch :139-143 → honest toast strings.ts:201, "+ New terminal" remains (TerminalTabs.tsx:57-81; test :210-223). 3 claimed tests exist :183,:195,:210. Coverage cite 115-146 ≈ actual onAdd 119-148.
- **Flow conformance:** FLW-01 fast-path edge NOT drawn — flows.md:29 still `D -->|+ New terminal - manual today, auto per IMP-01| E`, task analysis :18 says "currently manual". Flow drift.
- **Findings:**
  - [AUD-2026-07-23-17] (major) Auto-spawn cwd is `$HOME`, not the workspace root: createSession(ws.id) passes no opts (WorkspaceSidebar.tsx:140) → build_create_session forwards cwd:None (commands.rs:386-397) → sessiond defaults to $HOME (crates/sessiond/src/socket_server.rs:1190-1194). SCN-056 step 1 locks "cwd = workspace root"; manual path DOES pass the root (TerminalTabs.tsx:68-73). Cold-start test locks the bug (`toHaveBeenCalledWith("w3")`, WorkspaceSidebar.test.tsx:191) -> pass `createSession(ws.id, { cwd: dir, cols: 80, rows: 24 })` (or ws.roots[0]) and assert cwd in the test.
  - [AUD-2026-07-23-18] (minor) Misleading comment TerminalTabs.tsx:65-67 claims daemon defaults to roots[0] — false (sessiond $HOME default; correctly stated at commands.rs:460-462); likely seeded -17 -> correct the comment.
  - [AUD-2026-07-23-19] (minor) FLW-01 drift (flows.md:18,29): shipped conditional fast-path (cold-start auto / steady manual / spawn-fail → manual + toast) not drawn -> redraw D→E as the implemented conditional with the failure edge.
  - [AUD-2026-07-23-20] (minor) Status drift: SCN-056 `validated` despite code+tests+coverage (SCN-054 precedent uses `implemented`); ST-002 note likewise -> bump to `implemented` only after -17 lands (as-is, `validated` is accidentally honest).

## Batch 4: documented-unbuilt honesty check (SCN-047, SCN-048, SCN-049, SCN-050, SCN-055, SCN-057)

All six scenarios carry `Coverage: none yet` in the base. This batch verifies the claim is honest: no partial/phantom implementation, no dead chrome promising unbuilt behavior.

### SCN-047 — BLOCKED
- **Reason:** not built — scheduled S6b; `Coverage: none yet` confirmed accurate; no phantom runtime.
- **Evidence:** no CEO answer/continuation runtime anywhere (greps: question classification, auto-answer, decision-log — zero product hits). Only supervisor code is the SCN-046 config layer: crates/orchd-proto/src/lib.rs:279-295 (additive config type), crates/orchd/src/persistence.rs (validation/decode only), src/components/RulesetPanel.tsx (config section). `crates/sessiond/src/pty_supervisor.rs` is PTY process supervision — unrelated naming. The SCN-046 honesty note renders verbatim: src/components/RulesetPanel.tsx:761-762 (`ruleset-supervisor-pending`) with strings.rules.supervisor.pendingNote (src/strings.ts:326) = "The CEO acts on this once the orchestrator agent runtime lands (S6b)."

### SCN-048 — BLOCKED
- **Reason:** not built — scheduled S6b; `Coverage: none yet` confirmed accurate; no phantom escalation chrome.
- **Evidence:** all `escalat*` code hits are socket/handshake-retry logic (src-tauri/src/orchd_client.rs, socket_client.rs, lib.rs), not CEO escalation. App chrome has no needs-you badge: WorkspaceSidebar carries only the Inbox orphan count (src/components/WorkspaceSidebar.tsx:96,274-287) and Diagnostics count (:86,538). "Needs you" exists only as the SCN-004 Home section heading (src/strings.ts:128, src/components/HomeView.tsx:272-273) — shipped, documented behavior.

### SCN-049 — BLOCKED
- **Reason:** not built — scheduled S6b; `Coverage: none yet` confirmed accurate; no phantom hand-off machinery.
- **Evidence:** "hand-off/handoff" hits are channel-hand-off doc comments (src-tauri/src/orchd_client.rs:438, src-tauri/src/socket_client.rs:474) and a spec-phase note (crates/orchd/src/socket_server.rs:1736). No workflow-continuation logic, no next-task picker, no CEO-driven task transitions in orchd or UI.

### SCN-050 — BLOCKED
- **Reason:** not built — scheduled S6b; `Coverage: none yet` confirmed accurate; no phantom decision-log surface.
- **Evidence:** "decision log" greps — zero hits; "digest" hits are Sha256 (crates/orchd/src/ruleset_files.rs:19-25, crates/orchd/src/trust.rs:439-440). `strings.ext.log.thDecision` (src/strings.ts:692) is the MCP trust-audit table header (src/components/ext/InvocationLog.tsx:273) — a shipped, unrelated feature.

### SCN-055 — BLOCKED
- **Reason:** not built (validated, awaiting build); `Coverage: none yet` confirmed accurate; live Home still matches SCN-004 exactly.
- **Evidence:** src/components/HomeView.tsx implements SCN-004: sections Needs you / Running / Recently finished (:179-181,272-273), metrics strip workspaces·live·waiting, HomeGoals (SCN-005). No "Since you left" strip, no escalation cards, no hand-off rows, no CEO/spend tiles, no "open backlog →". strings.home (src/strings.ts:119-135) is SCN-004/005 copy only. Supersede integrity holds: SCN-004 index row still `implemented | 2026-07-22 PASS` (docs/ux/scenarios.md:12) — correctly NOT retired until v2 ships.

### SCN-057 — BLOCKED
- **Reason:** not built — draft, gated on spike A-9; `Coverage: none yet` confirmed accurate; no phantom auth plumbing.
- **Evidence:** zero hits for `ANTHROPIC_API_KEY`, `CLAUDE_CODE_OAUTH_TOKEN`, `CLAUDE_CONFIG_DIR`, `forceLoginOrgUUID`, auth-context concepts. ProjectPanel has exactly 8 tabs, no auth section (src/components/ProjectPanel.tsx:31-38). Existing Keychain code is scoped to the shipped MCP/connector secrets feature (crates/secrets/src/lib.rs:14-17 — two service names; src/ipc/orchd.ts:608,712,722; src-tauri/src/commands.rs:2416,2600,2621). Terminal spawn applies an env-hygiene allowlist (crates/sessiond/src/pty_supervisor.rs, spec §9.3) — no auth-env injection exists. **Build note for ST-043:** injected vars must extend that allowlist; Keychain half can reuse crates/secrets with a third service name.

**Batch 4 findings:** none — all six `none yet` claims honest, zero dishonest chrome.

## Flow conformance pass

| Flow | Verdict | Notes |
|---|---|---|
| FLW-01 | CONFORMS | Fast-path edge shipped (WorkspaceSidebar.tsx:132,138-144, degradation toast :142) but diagram still says "manual today, auto per IMP-01" and lacks the spawn-fail→manual error edge → AUD-2026-07-23-19 (fixed this change) |
| FLW-03 | NOT-BUILT (by design) | Home v2 nodes zero code; current Home = SCN-004 register (HomeView.tsx:240-299); carry-over pieces ready (`Go →` :202-208, HomeGoals). No contradicting partial v2 code |
| FLW-07 | CONFORMS | All nodes/edges wired incl. kill-fail→tab-removed-anyway (TerminalTabs.tsx:89-96), exited-wins StatusDot.tsx:8-24. Node `T` says unconditional "auto-activate"; code + SCN-013 say "if none active" → AUD-2026-07-23-21 (fixed this change). No auth nodes yet — SCN-057 unbuilt by design |
| FLW-13 | CONFORMS | Clean: 6 locked groups TasksList.tsx:16-26,100-110; fractional-rank single move :52-57; cascade-confirm names exact descendant count :62-71,454 → FK cascade persistence.rs:2494-2511; every error edge lands (create :490-491, flip-revert :402-410, rank/status :417-418, delete :458-459, orchdDown :360-361) |
| FLW-18 | CONFORMS + STALE-TAG | Every node/edge mapped (see Batch 1); `*(to-be)*` stale → AUD-2026-07-23-02 (fixed this change) |
| FLW-19 | CONFORMS (config half) + PARTIALLY-STALE; runtime NOT-BUILT (by design) | SET→ON leg shipped via SCN-046; Q..DEGR runtime awaits S6b, zero contradicting code. Wholesale to-be tag no longer accurate → AUD-2026-07-23-22 (fixed this change: tag split) |
| FLW-20 | CONFORMS + STALE-TAG | Every diagram node shipped via SCN-052/053; "Home spend tile" entry point awaits SCN-055 → AUD-2026-07-23-23 (fixed this change: tag dropped, entry point annotated pending SCN-055) |
| FLW-21 | CONFORMS + STALE-TAG | Every edge mapped (see Batch 3); tag stale → AUD-2026-07-23-15 (fixed); diagram edge `RB → D` wrong — code lands back in the editor with the recreated doc selected (DocsPanel.tsx:360-369), which is the better recovery → AUD-2026-07-23-24 (fixed this change: edge redrawn RB → ED) |

Cross-cutting: flows.md:7-8 header grouping "to-be flows (FLW-18..21, parts of FLW-03)" stale as a set — FLW-18/20/21 shipped, FLW-19 half-shipped, only FLW-03 v2 parts genuinely to-be (fixed this change).

## Heuristic findings (PRN)

Suggestions — opportunities, not blockers (contract: they change no scenario verdict unless they break a scenario).

- [PRN-01] (major) StatsView / range switch — `refreshStats` has no in-flight epoch guard: overlapping scans (rapid pill clicks, Refresh during scan) can resolve out of order and the stale reply overwrites the newer — figures render under the wrong range label with no indication. store.ts:1086-1126 (no request token, allSettled applied unconditionally); StatsView.tsx:170-180, :240-242 (Refresh not disabled while loading) -> epoch at scan start, discard non-latest replies; disable/queue pill+Refresh in flight.
- [PRN-01] (minor) StatsView loading/usage-failure — zero tiles render as data during initial scan and when usage fails (`empty` requires !loading && !usageError, StatsView.tsx:161; tiles :200-212) -> skeleton/"—" while loading; "—" usage tiles on usageError, git stays numeric. (Overlaps AUD-2026-07-23-09.)
- [PRN-09] (minor) StatsView error recovery placement — scenario promises "error card + Retry"; shipped is a warn note (:183-192) with no attached action, Retry lives as "Refresh" in the By-project header (:240-242) -> Retry inside the note or move Refresh adjacent.
- [PRN-03] (minor) StatsView long scan — no cancel affordance (StatsView.tsx:194; store.ts:1091-1126 no abort) -> cancel wired to the same epoch guard. (Same gap as AUD-2026-07-23-11.)
- [PRN-01] (minor) Heatmap axis hard-fixed to last 30 calendar days regardless of range (StatsView.tsx:156-159): with "7d" the 23 older cells read as real inactivity; label honest but contradicts the pill -> window by range, or always feed unfiltered 30d and say so.
- [PRN-06] (minor) TasksList unlabeled selects — create form stacks three visually unlabeled selects + tags input (TasksList.tsx:519-566); row selects side-by-side unlabeled (:280-307); aria-labels exist but sighted users must infer from current option text -> visible micro-labels or title attrs.
- [PRN-11] (minor) RulesetPanel CEO section fully expanded while toggle OFF (RulesetPanel.tsx:664-765); scope summary reads as active grant while disabled (:754-756) -> progressive disclosure behind the toggle; prefix summary "CEO disabled — would read/decide: …".
- [PRN-04] (minor) Inconsistent orchd-down gating inside one form — SCN-036 policy fields stay editable as drafts (RulesetPanel.tsx:625-662) while the CEO half disables every input (:674,697,715,728,743); same form, same Save, different editability -> one rule for the whole form (drafts-stay-live; the save gate protects correctness).
- [PRN-03] (minor) Silent draft clobber on store-wins — fresh view mid-edit (docs-changed/ruleset-changed push, reconnect rehydrate) resets dirty drafts without warning: RulesetPanel.tsx:431-446, DocsPanel.tsx:300-304; for docs, an agent writing the same file while the human types wipes the unsaved edit exactly when the external-change banner appears -> skip store-wins reset when draft dirty; let the banner mediate.
- [PRN-14] (minor) Docs preview renders block structure only; `**bold**`/links appear as raw source (markdown.ts:1-21 deliberate; DocsPanel.tsx:550) — reads as a rendering bug, rationale invisible -> support 3-4 common inline marks (JSX-only) or state the limitation under the Edit|Preview pill.

Clean surfaces: SCN-045 keep-awake and SCN-056 fast-path produced no heuristic findings (honest state machine, once-per-streak toast gating, attributable assertion name). Static trace only — visual-craft limited to token usage (consistent: semantic tone tokens, StatusDot convention).

## Practice compliance

| Practice | Verdict | How / why not |
|---|---|---|
| BP-003 (lean onboarding, grow by iteration) | applied | SCN-056 fast-path removes the manual step on cold start only (WorkspaceSidebar.tsx:119-148); empty-state single next action retained (:316-328) |
| BP-012 (defaults / suggest-for-me) | applied | "Recommended scope" preset safe-shell+file-write, editable, never invents caps (RulesetPanel.tsx:31,514-521,710-720); keep-awake default ON (power.rs:55-66); priority default normal (TasksList.tsx:392); stats range default 30d (store.ts:1084) |
| BP-040 (concrete activation metric) | deferred | recorded in foundation §6 as not-yet-instrumented; stats stack measures volume, not the activation event. Trigger: explicit instrumentation decision — the scanner/dashboard is now plausible carrier infrastructure |
| BP-041 (aha as a sequence) | deferred | same recording; SCN-056 engineers the aha step, nothing measures it |
| Remaining BP catalog (paywall/pricing/push/winback/store sets) | rejected (recorded, user-owned) | free local macOS dev tool — N/A by product class; not re-litigated |

## Coverage pass

Chain integrity for the scoped slice (deterministic base: `python3 docs/ux/lint.py` → OK, 0 errors 0 warnings at audit time; lint.py restored this session from the canonical super-ux `ux_lint.py`).

- Every scoped story has its scenario: ST-033→045, ST-034→046/047/048, ST-035→049, ST-036→050, ST-037→051, ST-038→052, ST-039→053, ST-041→054, ST-042→055, ST-002→056, ST-043→057. ST-040 traced by SCN-052 (verified in code — COV-01 closure observable, primitives.tsx:437,522 → StatsView.tsx:170,219).
- Journey coverage: JRN-10 stages #1-#6 and JRN-11 #2-#4 all have scenarios in scope; no scoped journey stage without a scenario.
- Supersede integrity: SCN-004 correctly NOT retired while SCN-055 unbuilt (index row `implemented | 2026-07-22 PASS`, scenarios.md:12); live Home matches SCN-004 (HomeView.tsx).
- Orphan check: no scoped scenario without a story; no phantom coverage — all six `none yet` claims verified honest (Batch 4).
- Gap closed this session: SCN-057 had no flow for its bind/test/clear path (FLW-07 covers spawn lifecycle only) → FLW-22 added; SCN-057 Traces updated in the same change.
- Broken-link repair: foundation.md §6 pointed at a versioned plugin-cache path (machine-specific, 404) → replaced with a stable plain-text reference; lint now fully green.

## Findings register

| # | Scenario | Severity | Finding | Suggested fix |
|---|----------|----------|---------|---------------|
| AUD-2026-07-23-17 | SCN-056 | **major** | Fast-path auto-spawn cwd = `$HOME`, not the workspace root (WorkspaceSidebar.tsx:140 → commands.rs:386-397 cwd:None → sessiond $HOME default); test locks the bug | pass `{ cwd: dir }` (or ws.roots[0]) + assert cwd in test |
| AUD-2026-07-23-25 | SCN-052 | **major** | Range-switch race: no epoch guard in refreshStats (store.ts:1086-1126) — stale scan reply overwrites newer; figures render under wrong range label (breaks "figures render for the chosen range"; also [PRN-01]) | epoch token, discard non-latest; gate pill/Refresh while in flight |
| AUD-2026-07-23-07 | SCN-052 | **major** | Per-agent cut absent across chain (stats.rs:38-54 buckets day×cwd; view per-project only) — scenario element + ST-038 AC1 unobservable | add agent/family dimension, or descope "per agent" explicitly |
| AUD-2026-07-23-11 | SCN-053 | **major** | "Slow scan cancellable" not implemented — no cancel affordance/abort path (store.ts:1091-1126, stats.rs:466-509) | cancel via same epoch guard, or amend scenario |
| AUD-2026-07-23-03 | SCN-046 | minor | Step 2 promises caps "(spend, calls/min)"; summary shows spend only while trust-policy ratePerMin exists unsurfaced (RulesetPanel.tsx:706-708; orchd-types.ts:196) | surface ratePerMin or amend step 2 |
| AUD-2026-07-23-08 | SCN-052 | minor | Empty predicate ignores git (StatsView.tsx:161) — usage-empty range hides real git output | empty only when both sources empty |
| AUD-2026-07-23-09 | SCN-052 | minor | usageError + empty days renders 0-tiles beside alert — zeros styled as data (also [PRN-01] minor) | dash usage tiles on error |
| AUD-2026-07-23-10 | SCN-052 | minor | Precondition names "MCP invocations" source; only session JSONL scanned | wire source or drop from precondition |
| AUD-2026-07-23-12 | SCN-053 | minor | stats_git `unwrap_or_default` (stats.rs:508) — worker failure = fake empty success | mirror stats_usage error honesty |
| AUD-2026-07-23-13 | SCN-053 | minor | Freshness stamp from usage.asOf only; git table stampless when usage fails | stamp from refresh completion |
| AUD-2026-07-23-14 | SCN-053 | minor | gitReason captured, never rendered — honest per-root reason dies in view | tooltip on no-git cell or delete field |
| AUD-2026-07-23-18 | SCN-056 | minor | Misleading comment TerminalTabs.tsx:65-67 ("daemon defaults to roots[0]" — false); likely seeded -17 | correct comment |
| AUD-2026-07-23-20 | SCN-056 | minor | Status `validated` despite code+tests — bump to `implemented` only after -17 lands (kept as-is: accidentally honest) | flip after fix |
| AUD-2026-07-23-01 | SCN-045 | minor | Coverage-cite drift (5 files) | refresh cites |
| AUD-2026-07-23-04 | SCN-046 | minor | Coverage-cite boundary nits (4 ranges) | widen cites |
| AUD-2026-07-23-05/06 | SCN-051 | minor | Coverage-cite drift (proto verb moved to :1369-1372; commands/persistence ranges) | refresh cites |
| AUD-2026-07-23-16 | SCN-054 | minor | Coverage-cite drift (strings/proto/persistence/store ranges; "19"→20 tests) | refresh cites |
| AUD-2026-07-23-02 | FLW-18 | minor | Stale `*(to-be)*` tag | **fixed this change** |
| AUD-2026-07-23-15 | FLW-21 | minor | Stale `*(to-be)*` tag | **fixed this change** |
| AUD-2026-07-23-19 | FLW-01 | minor | Fast-path edge not drawn; "manual today" stale; missing spawn-fail edge | **fixed this change** |
| AUD-2026-07-23-21 | FLW-07 | minor | Node `T` unconditional "auto-activate" vs code/SCN-013 "if none active" | **fixed this change** |
| AUD-2026-07-23-22 | FLW-19 | minor | Wholesale to-be tag vs shipped config half | **fixed this change** (tag split) |
| AUD-2026-07-23-23 | FLW-20 | minor | Stale to-be tag; Home-spend-tile entry point needs pending-SCN-055 annotation | **fixed this change** |
| AUD-2026-07-23-24 | FLW-21 | minor | Diagram `RB → D` vs code's better `RB → ED` recovery | **fixed this change** |

Heuristic suggestions (no verdict impact): PRN-09 retry placement, PRN-03 draft-clobber on store-wins (RulesetPanel.tsx:431-446, DocsPanel.tsx:300-304), PRN-01 heatmap-vs-range mismatch, PRN-06 unlabeled selects, PRN-11 CEO section disclosure, PRN-04 split orchd-down gating in one form, PRN-14 inline marks unrendered — see Heuristic findings.
