# UX Audit — 2026-07-22 (coverage / chain)

- **Scope:** coverage — audits the traceability chain, not the code.
- **Method:** cross-file parse of `foundation.md` ↔ `scenarios.md`, both directions; plus a reverse sweep of shipped UI capability vs. scenarios.
- **Base version:** working tree on `nightbuild` atop `4337655` (foundation.md + ux-contract-v2 scenarios staged this session).

## Summary

- **Forward chain (WHY → scenarios): COMPLETE — 0 orphans.**
  - 3 personas, each with ≥1 job.
  - 9 JTBD, each with ≥1 user story **and** ≥1 journey.
  - 32 stories, each with ≥1 scenario; **every `must`/`should` story is covered** (only `could` stories carry lower-priority surfaces, all still covered).
  - 44 scenarios, each tracing to ≥1 story + job; all 44 are also referenced by ≥1 journey stage.
  - Index ↔ bodies match; all persona refs (P-01..03) defined; all statuses legal.
- **Reverse chain (code → scenarios): 1 finding** — two shipped, unit-tested design-system atoms are wired into no view and therefore serve no scenario/story/job (orphan capability).
- **Unmet journey opportunities:** 3 `suggestion`-level gaps where a journey stage names an opportunity the product does not yet meet (future work, tracked as foundation assumptions — not chain breaks).
- **Verdict totals: PASS 0 / PARTIAL 0 / FAIL 0 / BLOCKED 0** (coverage scope emits findings against layer IDs, not per-scenario code verdicts). Chain health: **green**, with one inventory finding to resolve.

## Chain coverage matrix

| Layer | Count | Fully traced? | Notes |
|-------|-------|---------------|-------|
| Personas (P) | 3 | ✅ | P-01 (37 scns), P-02 (SCN-001/002/003), P-03 (SCN-019); all appear in ≥1 JTBD |
| Jobs (JTBD) | 9 | ✅ | each has ≥1 story and ≥1 journey |
| Journeys (JRN) | 9 | ✅ | each belongs to a persona×job and cites ≥1 scenario |
| Stories (ST) | 32 | ✅ | each has ≥1 scenario; all must/should covered |
| Scenarios (SCN) | 44 | ✅ | each traces to a story+job; all referenced by a journey |

## Journey-stage coverage

Every journey stage with a **product touchpoint** cites a scenario; `before`/`after`
stages that live outside the product (install, "returns after time away") correctly
have none. The following stages name an **opportunity the product does not yet
deliver** — these are coverage *opportunities*, not missing scenarios:

- **JRN-02/#5** ("returns to Home for the next" ) → no persistent "needs you"
  signal outside the Home view. Foundation assumption **A-1**.
- **JRN-01/#5** (first-run capture) → ⌘K is not taught anywhere in the empty
  state; discovery is left to chance. Foundation assumption **A-3**.
- **JRN-07/#3** (knowledge graph) → manual-upkeep value unvalidated (ST-025 is
  `could`). Foundation assumption **A-4**.

## Findings register

| # | Layer refs | Severity | Finding | Suggested fix |
|---|-----------|----------|---------|---------------|
| AUD-2026-07-22-COV-01 | (reverse) `src/ui/primitives.tsx:437` SegmentedPill, `:522` Heatmap | minor | Both atoms are exported and unit-tested (`primitives.test.tsx`) but imported by **no view** (0 refs across `src/components/`, `src/App.tsx`). Their own doc-comments name intended surfaces («Overview \| Models» / «All \| 30d \| 7d» switcher; "blue density grid — the data voice") that were never built. Shipped capability serving no scenario/story/job. | Either (a) wire them into a real surface — e.g. SegmentedPill onto the ProjectPanel 7-tab bar or a research «All\|30d\|7d» range switcher; Heatmap onto command-history/session-activity density — and add/extend the owning scenario + its `Traces`; or (b) record them explicitly as intentional design-system inventory (atoms built ahead of use) so they are not mistaken for dead code. Not silently accepted. |
| AUD-2026-07-22-COV-02 | JRN-02/#5, A-1 | suggestion | Attention triage is pull-only; no persistent "needs you" count outside Home. A blocked agent is invisible until the operator reopens Home. | Consider a sidebar/badge "needs you (N)" surface; would need a new scenario. Validate A-1 first. |
| AUD-2026-07-22-COV-03 | JRN-01/#5, A-3 | suggestion | First-run empty state does not teach the ⌘K capture hotkey (JTBD-04 discovery). | Add a one-line hotkey hint to the empty state; extend SCN-001. Cheap; test with a first-run user. |
| AUD-2026-07-22-COV-04 | JTBD-09, BP-040/BP-041 | suggestion | No activation metric instrumented. "Reached a live terminal in a real workspace" (JRN-01/#4) is the natural aha but is not measured. | Instrument setup→aha→habit; enables future funnel work. Product-appropriate subset of the BP catalog (see foundation §6). |

## Recommended next actions (prioritized)

1. **Resolve COV-01** (the only real chain finding): decide wire-in vs. inventory for SegmentedPill/Heatmap; if wire-in, it becomes a scenario-first task (draft scenario → build → audit).
2. **Validate the inferred WHY layer** with a real user — the whole foundation is reverse-engineered (foundation §5 assumptions A-1..A-5). Highest-leverage: A-2 (restart-survival is the trust anchor).
3. Treat COV-02/03/04 as backlog opportunities, not blockers.

## Definition of done

- Chain audited both directions; forward chain 0 orphans; reverse sweep surfaced 1 orphan-capability finding, not swallowed.
- Findings reference layer IDs per the coverage-audit contract.
- Recommendations handed to planning; the honest state is: **chain is green, WHY layer awaits human validation, two atoms await a home.**
