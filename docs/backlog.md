# Backlog — accepted-deferred findings (canonical)

Rule (normative, see CONTRIBUTING.md): any accepted-deferred finding MUST land here
in the same change that defers it. Gitignored ledgers are working notes, never the record.

Severity: P1 security/correctness · P2 robustness/ops · P3 polish.
Status: open · in-progress · done (row stays, link the closing commit).

| ID | Severity | Area | Summary | Origin | Owner slice | Status |
|----|----------|------|---------|--------|-------------|--------|
| BL-1 | P1 | daemon/security | Enforce `DYLD_*`/`LD_*` denylist on `env_overrides` before applying (allowlist is a default, not a ceiling, until then) | Audit A11; spec §16 decision | Pre-S6 hardening | done (S-EXT T16 — shared `bpa_daemon_core::env_filter::strip_dangerous_env` case-sensitive `DYLD_*`/`LD_*` prefix denylist, applied to sessiond's `env_overrides` before merge in `crates/sessiond/src/socket_server.rs` `resolve_session_spec`; commit a9a2249. The T16-review follow-up additionally hardened the *other* spawn path: `bpa-mcp`'s `build_stdio_transport` now `env_clear()`s the stdio child (no ambient inheritance) and orchd's `build_stdio_env` (`crates/orchd/src/mcp/mod.rs`) builds the child's COMPLETE env = filtered(orchd-ambient ∪ DB `server.env`), so no `DYLD_*`/`LD_*` from EITHER source can reach a stdio MCP child either) |
| BL-2 | P1 | webview/security | Set restrictive CSP in `tauri.conf.json` (currently `csp: null`); LLM egress core-only rule | Audit A19 | Pre-S6 hardening | open (re-confirmed in the 2026-07-19 audit — see `docs/qa/2026-07-19-code-audit-test-plan.md` §2.6/F1. Feasibility de-risked: the frontend has ZERO HTML-injection sinks — no `dangerouslySetInnerHTML`/`innerHTML=` (now regression-guarded by `src/__tests__/smoke.test.ts`), index.html has only an external module script, and only inline *styles* need `'unsafe-inline'` — so a strict CSP (`default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; …`) is workable. Deliberately NOT auto-landed in the audit fix loop: a wrong `connect-src` bricks the Tauri v2 IPC and jsdom tests can't catch it, so it needs a real-app GUI smoke to verify — kept as a designed slice, not a drive-by. The egress-lockdown half still needs the "core-only" design.) |
| BL-3 | P1 | daemon/security | `bpa.db` file mode 0600 + purge-on-delete (scrollback carries secrets) | Audit A18 | Pre-S6 hardening | open |
| BL-4 | P2 | daemon | Session/workspace deletion + retention (20 exited/workspace, 30-day TTL, cascade with consent) + `SessionLimitReached` cap | Audit A15, A21; spec P3 | next sessiond cycle | open (re-targeted T21: this is `bpa-sessiond` terminal/session-domain work — S3 deliberately did NOT touch sessiond behavior (daemon-core extraction kept it byte-identical), so "S3" was never the right owner slice; moves to whichever cycle next revisits sessiond) |
| BL-5 | P2 | frontend | Reconnect stale-pane UX: mark stale until re-attached; dim/disable panes while disconnected | Audit A22 | S2 UI pass | open (unaddressed by S2 — Toast infra landed this cycle (BL-6) but this row is about pane-level staleness, a different surface; still open) |
| BL-6 | P2 | frontend | Error-surfacing implementation: code→message table, toasts, catch on all fire-and-forget invokes (incl. `void manager.attach`) | Audit A23 | S2 UI pass | open (Toast infra + honest fs/upgrade-adjacent toasts landed this cycle: S2 Task 9 built `toast`/`showToast`/`dismissToast` + `<Toast/>`; Tasks 10–13 wired it into real fire-and-forget failure paths — `FileTree` (list/create/rename/delete/reveal/open/add-root), `FilePreview` (read failure), `CommandStrip` (history fetch failure), the terminal-manager linkHandler.activate wiring (click-resolves-outside-root/not-found; the link-provider itself is a pure store-free resolver). `UpgradeDialog`/`DaemonBanner` intentionally keep their own pre-existing in-dialog/banner error patterns (design-system.md's dialog-failure convention, a different atom by design, not an oversight). Still open: a code→message table (current call sites format ad hoc strings) and the remaining fire-and-forget invokes outside the fs/terminal-link/command-strip surface) |
| BL-7 | P2 | e2e/daemon | Daemon-restart rehydration e2e phase (SIGTERM daemon, relaunch, assert inactive rehydrate + scrollback) + persistence-degraded Push event | Audit A12 | Protocol v2 | done (Task 12/12r — e2e phase 5, `tests/e2e/survive-restart.mjs`, green; persistence-degraded Push event remains a separate, still-open follow-on if ever needed) |
| BL-8 | P2 | daemon | Scrollback flush dirty-check (skip unchanged rings) + DB write-architecture review | Audit A24 | next sessiond cycle | open (re-targeted T21: `bpa-sessiond` scrollback-flush internals — S3 deliberately did NOT touch sessiond behavior; "S3" was never the right owner slice) |
| BL-9 | P3 | daemon | `flush_scrollback_once`: batch per-tick DB-lock acquisition (head-of-line latency) | Final review deferral | next sessiond cycle | open (re-targeted T21: same `bpa-sessiond` scrollback-flush surface as BL-8 — S3 did not touch it) |
| BL-10 | P3 | daemon | `pty_supervisor` sink MutexGuard held across `send()` — hardening vs future bounded channel | Final review deferral | any | open |
| BL-11 | P2 | daemon | Escaped-descendant stream: keep cancel reachable after `remove_session`; Output-after-ChildExited when a descendant holds the PTY slave | Truncation-fix verification | Protocol v2 | open (re-evaluated under Pv2 multi-sink fan-out — `crates/sessiond/src/pty_supervisor.rs` `Shared.sinks: Mutex<Vec<(sub_id, Sink)>>`: the reasoning is unchanged and now applies PER-SINK — `remove_session` still lets each subscriber's forwarder drain gracefully rather than cancel, and a descendant holding the PTY slave open after exit can still emit Output-after-ChildExited to any one subscriber independently of the others; no fix, still deferred) |
| BL-12 | P3 | daemon | Stale attach entry when child exits during in-flight attach Replay send (count over-reports until conn close) | Truncation-fix verification | any | open |
| BL-13 | P3 | core/tests | Preflight call-site wiring untested (`preflight_cwd`/`preflight_workspace_root` calls in command fns need a State-driven test) | Truncation-fix verification | any | open |
| BL-14 | P2 | frontend | `applyReplay` without `term.reset()` → duplicated scrollback on any re-attach | A1 verification; chip task_ada4835d | S2 UI pass | done (S2 Task 9 — `terminal-manager.ts::applyReplay` now calls `term.reset()` before resize+write on EVERY Replay, including re-attach; test drives attach→Replay→reset→re-attach→Replay and asserts reset-before-write ordering + the xterm buffer holds only the second Replay's bytes, not both concatenated) |
| BL-15 | P3 | core | Cross-layer stale-channel windows: 30 s-hang double-replay; unconditional `remove_attachment` on failed attach | Attach-dedup verification | Protocol v2 | open |
| BL-16 | P2 | test-infra | `singleton.rs` env-mutation race (needs process-wide env lock); once-seen attach parallel flake; real-shell integration tests silently no-op when the shell binary is absent — CI must assert they ran (ci.yml does since this cycle) | Ledger CI-TODO | CI hardening | open |
| BL-17 | P2 | CI | Coverage gate ≥80 % sessiond enforced in CI (job added this cycle — verify it stays blocking); local run optional | Spec §14.3; audit A20 | this cycle → done when CI green | done (CI `coverage` job is present and blocking in `.github/workflows/ci.yml`; verified green on 0.10.0 during the 2026-07-24 audit baseline — `OK: bpa-sessiond coverage >= 80%` / `OK: bpa-orchd coverage >= 80%`) |
| BL-18 | P3 | bundle | Prune ~28 unused iOS/Android icon assets from `src-tauri/icons/` | Final review deferral | any | open |
| BL-19 | P3 | release | Tauri auto-updater channel (manifests, hosting) — manual DMG until then | Owner decision D4 | post-S2 | open |
| BL-20 | P1 | agents/security | macOS Keychain storage for provider API keys (never SQLite/config/logs) | Audit A9; spec §16 | S6a | done (S-EXT T1 — `bpa-secrets` (`crates/secrets/src/lib.rs`), a thin `security-framework::passwords` wrapper (set/get/delete generic password, fixed service prefix `ai.builderpro.desktop`), is the ONLY Keychain caller in the app; S-EXT's MCP bearer tokens, OAuth access/refresh tokens, and connector api-keys all route through it (`orchd.db` stores only a Keychain ref, never the secret bytes, D4). S6a's own provider-API-key storage (LLM providers) is expected to reuse this same crate verbatim when it lands — the pattern is proven, not a per-slice one-off) |
| BL-21 | P2 | daemon/ops | Log rotation: appender is `rolling::never` (single unbounded `sessiond.tracing.log`) — switch to daily or size-capped | Runbook truth pass (this cycle) | any | open |
| BL-22 | P1 | mcp/security | MCP hardening: tool-result prompt-injection mediation, per-server connect consent, stdio-server execution consent, spend caps via the trust layer | Vision-v2 audit V13 | S-EXT | done (S-EXT T5/T16/T18 — `crates/orchd/src/trust.rs`'s single pre-dispatch choke-point covers per-server connect consent (fingerprint = URL, re-prompt on change), a DISTINCT stdio-exec consent (fingerprint = resolved-binary sha256, falls back to a command-string hash), the per-tool allowlist, spend/rate policy caps (60s rolling window, most-specific-scope-wins, a spend cap binds only when a server reports cost), and an append-only `audit_log`. The "tool-result prompt-injection mediation" half of this row's original title is satisfied ONLY at the tagging/quarantine layer — every `mcp_artifact` (from `McpCallTool` AND `ConnectorInvoke`) is stored `is_untrusted=1`; the ACTIVE mediation step (an agent boundary that reads the flag and rewrites/quarantines accordingly) has no agent to run in yet — split out honestly as new row BL-74, owner S6b, rather than silently folded into this "done") |
| BL-23 | P3 | ux/ops | Remote escalation channel — answer a hot question away from the Mac (macOS notification only in v0.x; Telegram/mobile web later) | Owner decision Q3 | post-S6c | open |
| BL-24 | P2 | data/security | Ingested-telemetry retention + redaction: prod logs/errors + metrics carry PII/secrets — store home, redaction, retention, purge-on-project-delete | Charter telemetry class; Q15 | S9a | open |
| BL-25 | P3 | product | First-class Experiment entity (hypothesis / budget / metric / verdict); v1 uses Tasks tagged `experiment` | Owner decision Q11 | S8 | open |
| BL-26 | P2 | orchd | StepRun payload-thinning job: full I/O payloads 14 days → thinned to metadata (summaries + sizes + hashes); 50 MB/run cap with honest truncation markers | Owner decision Q15 | SW1 | open |
| BL-27 | P2 | orchd/security | Keychain access for `bpa-orchd` unattended runs while the screen is LOCKED — resolve before the first unattended MCP/provider call | ADR-HOST sub-question | S6b/SW2 | open (re-targeted from S-EXT: S-EXT spec §1/§10 named this an explicit non-goal — v1's MCP/connector flows are all interactive/screen-unlocked; the row only bites once something calls Keychain-backed MCP/connector code with NO owner present, which is exactly "the first unattended MCP call" S6b/SW2's scheduler + agent runtime introduce — moves with that work, not before it exists) |
| BL-28 | P3 | tests | Shared test-support MockLaunchctl: `commands.rs` tests structurally duplicate `launchd::tests::MockLaunchctl` (private) — extract a shared test-support module | Task 10a review | any | open |
| BL-29 | P3 | ux/a11y | Explicit `:focus-visible` CSS (2px accent ring) app-wide — design-system promises it, components rely on UA default; also `within()`-scope duplicate «Refresh» accessible names (banner + dialog co-mounted) in future full-app tests | Task 10b review | S2 | done (S2 Task 9 — new `src/index.css` defines app-wide `:focus-visible { outline: 2px solid #2f81f7; outline-offset: 2px }` + the standard `:focus:not(:focus-visible) { outline: none }` companion rule, imported in `main.tsx`; the `within()`-scope duplicate-accessible-name observation is a note for whoever writes the future full-app test, not a code defect this task touches) |
| BL-30 | P2 | daemon/perf | Cold-rehydrate loads ALL persisted sessions' scrollback into RAM at boot (≤256 KiB × N sessions) — fine at v0.x scale; consider lazy per-session load before workspaces with many dead sessions | Task 12r | next sessiond cycle | open (re-targeted T21: `bpa-sessiond` cold-rehydrate — S3 deliberately did NOT touch sessiond behavior; "S3" was never the right owner slice) |
| BL-31 | P3 | daemon | `command_events` not reconciled on cold-rehydrate (rehydrated sessions carry no in-memory event queue; persisted rows remain readable) — reconcile when a consumer appears | Task 12r review | SW1 | done (S2 T3/T4 — the consumer arrived: `Request::GetCommandEvents` → `Db::list_command_events` (`crates/sessiond/src/persistence.rs`) reads `command_events` rows straight from SQLite by `session_id`, with no in-memory event queue in the path at all — a rehydrated (replay-only) session's history is exactly as readable as a live one's. The deferred "reconcile when a consumer appears" concern is moot by construction, not by a fix) |
| BL-32 | P3 | daemon | DaemonShutdown Ack races process exit at PROCESS scope: serve()/run() never join per-connection tasks, so on a zero-session fast drain the enqueued Ack (and a second client's in-flight reply) can be dropped before the writer flushes; e2e passes because real drains are slower — add a bounded quiesce/join of client tasks before exit | Pv2 final review (Minor #6) | Pv2.1 | open |
| BL-33 | P3 | daemon | Replay-only attach during the reap-vs-reader-drain window can deliver a permanently truncated Replay (ChildExited broadcast before the reader finishes draining the PTY; ReplayOnly entry never receives the tail) — tight race, bounded loss; align reap ordering or delay inactive-attach until reader-drain completes | Pv2 final review (Minor #8) | Pv2.1 | open |
| BL-34 | P2 | core/upgrade | Non-force boot `kickstart()` + no build comparison: a protocol-COMPATIBLE but stale daemon binary is never restarted after an app update (the running v2 daemon keeps serving; its handshake `build` string is logged, never compared to the bundle) → daemon bug/security fixes don't take effect until the next daemon restart. Add build-string comparison → non-destructive "restart to update" prompt (reuses consent dialog). Deliberately NOT auto-force-restarting (that was the removed `-k` data-loss bug). **Applies to `bpa-orchd` too as of S3 (T21 annotation):** `bring_up_orchd` (`src-tauri/src/lib.rs`) does the identical non-force boot `kickstart()` with no build comparison as `bring_up_daemon` — a stale-but-compatible orchd binary is equally never restarted after an app update; the eventual fix should cover BOTH daemons in one pass, not just sessiond | Pv2 final review round-2 (Important) | next daemon-upgrade cycle | open (re-targeted from S-EXT: this is `bring_up_orchd`/`bring_up_daemon` boot-choreography work in `src-tauri/src/lib.rs`, unrelated to S-EXT's egress/Keychain/MCP scope — S-EXT never touched `lib.rs`'s kickstart path, so "S-EXT" was never the right owner slice; moves to whichever cycle next revisits the daemon-upgrade/kickstart choreography for either daemon) |
| BL-35 | P3 | daemon/protocol | KillSession honest-close of an inactive session Acks only the requester — no `Push::SessionRemoved` broadcast, so under multi-client (Pv2 §5.2) other connections keep showing the killed session. Add an additive `Push::SessionRemoved{session_id}` variant + broadcast; only bites once a second concurrent GUI subscriber exists (post-S6) | Pv2 final review round-2 (Minor, D3) | S6 | open |
| BL-36 | P3 | daemon | KillSession on an inactive session Acks even if `Db::delete_session` fails (logs a warn); the surviving row reappears in ListSessions and resurrects on next restart, and a repeat KillSession in the same lifetime returns NoSuchSession. Narrow DB-I/O-failure edge; make the Ack conditional on the delete or retry | Pv2 final review round-2 (Minor, D3 residual) | Pv2.1 | open |
| BL-37 | P3 | ui | `hydrated` store flag never resets once true: a mid-session daemon swap to an incompatible build shows the consent dialog with a stale pre-swap session count (approximately-right, not the boot-incompatible 0-count bug which is fixed). Recompute/mark count-staleness on incompatible transition | Pv2 final review round-2 (Minor, F2 residual) | Pv2.1 | open |
| BL-38 | P3 | daemon | `Push::ChildExited` now broadcast from inside the async final-flush task (after up to 3 `db.lock().await`), so on KillSession the client can see the Ack before ChildExited, and a contended DB mutex delays the exit notification; delivery still eventual (e2e tolerates Ack-before-Push). Decouple the exit push from the flush task if ordering/latency ever matters | Pv2 final review round-2 verify (Minor) | Pv2.1 | open |
| BL-39 | P3 | core/upgrade | Initial-connect incompatible escalation needs 8 CONSECUTIVE transient handshake failures within a bounded attempt budget; a single interleaved connect-refused (Io) mid-run resets the counter, so a pathological v1 daemon that intermittently refuses could avoid escalation until the next app launch (the unbounded reconnect loop has no such fragility). Make the initial-path budget resilient to interleaved Io | Pv2 final review round-2 verify (Minor, R1 residual) | Pv2.1 | open |
| BL-40 | P3 | tests/ci | Daemon/core real-PTY tests use fixed wall-clock deadlines (recv/reap/handshake) that can flake under full-workspace parallel oversubscription or a transiently loaded host (not a product bug — always green in isolation; `natural_exit` reap/collection deadlines bumped to 30s as the first pass). Systematically audit remaining fixed-deadline/fixed-sleep assertions → condition-wait or generous caps; consider serializing PTY-spawning tests | Pv2 final gate (test-robustness) | any | open (openpty half addressed 2026-07-19, A1: `Supervisor::create` now retries `openpty` on transient OS resource exhaustion (EAGAIN/ENFILE/EMFILE) with bounded linear backoff — the `openpty code -6` panic that broke `cargo test --workspace` and the coverage gate under oversubscription no longer occurs. The fixed-wall-clock-deadline audit across the remaining recv/reap/handshake assertions is still open.) |
| BL-41 | P3 | frontend | Chip-style triple-duplication: `MONO_FONT` is redefined verbatim in 5 files (`src/App.tsx`, `src/components/FileTree.tsx`, `src/components/FilePreview.tsx`, `src/components/HomeView.tsx`, `src/components/CommandStrip.tsx`); `statChipStyle` (`App.tsx`) and `chipBaseStyle` (`CommandStrip.tsx`) are two independent, near-identical chip style objects. Extract one shared `Chip` atom (design-system.md §5 already documents the Chip contract; no code currently reuses it) | Task 12 review (S2) | post-S2 polish | open |
| BL-42 | P3 | frontend | "N live" label diverges by view: `HomeView.tsx`'s stats strip counts `liveCount = waitingCount + running.length` (waiting sessions count as "live", spec §6.2 "the owner's context across ALL projects"), while `WorkspaceStatsChips` (`src/App.tsx`) labels only `running`-and-not-waiting sessions "live" (waiting gets its own separate chip). Both are internally consistent with their own view's semantics but the shared word "live" means two different session sets — reconcile the wording or accept and document the split | Task 12 review (S2) | post-S2 polish | open |
| BL-43 | P3 | frontend/a11y | Workspace stat chips (`WorkspaceStatsChips` in `src/App.tsx`) use `aria-pressed` on a click-to-expand-detail-panel interaction — that's a disclosure pattern (spec §6.3 "click expands a small detail panel"), not a toggle-button pattern; the correct ARIA is `aria-expanded` + `aria-controls` pointing at the detail panel's id | Task 12 review (S2) | post-S2 polish | open |
| BL-44 | P2 | frontend | `FilePreview` has no workspace-scoping: `selectedFile` (`src/store/store.ts`) is never cleared on workspace switch, so switching workspaces can leave a stale file from the PREVIOUS (now inactive) workspace showing in the right rail. Fix: clear `selectedFile` on workspace switch, OR gate `FilePreview`'s render on `selectedFile.root` being a member of the active workspace's `roots` | Task 12 review (S2) | S2 follow-up | open |
| BL-45 | P3 | frontend | `FileTree`'s per-(root,rel) `treeCache` (`src/store/store.ts`) is never invalidated when a directory is deleted or renamed via the tree's own context menu — the OLD rel path's cached children entry becomes an orphan (harmless until something re-navigates to the stale path, then shows ghost content until the next `fs://changed` point-refresh) | Task 10 review (S2) | S2 follow-up | open |
| BL-46 | P3 | frontend | Root nodes in `FileTree` display `basename(root)` (`src/components/FileTree.tsx`) as the visible label; two workspace roots that happen to share a basename (e.g. two different repos both named `api`, under different parents) are visually indistinguishable in the tree — no full-path disambiguation or tooltip today | Task 10 review (S2) | S2 follow-up | open |
| BL-47 | P3 | frontend | Delete confirmation in `FileTree` uses the native `window.confirm` (`src/components/FileTree.tsx:359`) rather than the themed dialog/modal atom design-system.md §5 already defines — visually breaks the app's chrome for the one destructive action in the explorer | Task 10 review (S2) | S2 follow-up | open |
| BL-48 | P3 | frontend/perf | `invalidateDirs` (`src/store/store.ts`) always allocates a fresh `treeCache` even when none of the given `(root, rels)` match an existing cache entry — an avoidable re-render of every `FileTree` consumer subscribed to the cache on every `fs://changed` tick, including no-op ones | Task 8 review (S2) | post-S2 polish | open |
| BL-49 | P3 | frontend | Terminal file links (`src/terminal/link-provider.ts`, spec §6.5/D9) v1 scope gaps, all deliberate/documented in-module, not accidental: (1) a path token that soft-wraps across two rendered terminal lines is never detected (matching is per-line only); (2) a single-segment absolute path like `/etc` never matches `TOKEN_PATTERN` (requires ≥2 segments after the leading `/`, to avoid over-linkifying); (3) `~/`-prefixed tokens are detected then explicitly skipped (the resolver is store-free, has no `$HOME`) rather than resolved | Task 13 (S2 spec D9 v1 scope) | S2 follow-up | open |
| BL-50 | P3 | core/fs | fs_explorer delete/rename/move act on a symlink's RESOLVED target (canonicalize follows the final component), not the link itself: deleting an in-root `link.txt`→`real/data.txt` trashes the target + leaves a dangling link; rename/move relocate the pointee. Within-root (outside-root symlink still rejected), delete is Trash-reversible; niche. Consider lstat/no-follow on the final component | S2 final review (Minor) | S4/S5 window | open (re-targeted T21: `src-tauri/src/fs_explorer.rs` — S3 shipped the orchd domain store, not fs; it never touched this file. Moves to whichever of S4/S5 next touches core-local file I/O) |
| BL-51 | P3 | core/fs | fs_watcher misroutes live changes under NESTED/overlapping roots (`["/p","/p/sub"]`): a change under `/p/sub` matches the first root `/p` only, so the `/p/sub` tree isn't invalidated. Also add_workspace_root doesn't reject a root nested in another. Best-effort watcher; reject nested roots or match the most-specific root | S2 final review (Minor) | S4/S5 window | open (re-targeted T21: `src-tauri/src/fs_watcher.rs` — S3 doesn't touch fs; same S4/S5 window as BL-50/BL-52) |
| BL-52 | P3 | core/fs | fs_watcher root-only `.gitignore` drops a REAL change to a file re-included by a NESTED negation (`sub/.gitignore` = `!debug.log` under root `*.log`): list_dir shows it (nested-aware) but the watcher filters its event → stale until manual refresh. Documented worst case; make the watcher matcher nested-aware or drop the root-only filter | S2 final review (Minor) | S4/S5 window | open (re-targeted T21: `src-tauri/src/fs_watcher.rs` — S3 doesn't touch fs; same S4/S5 window as BL-50/BL-51) |
| BL-53 | P3 | orchd/product | Un-archive project verb: `ArchiveProject` exists (`OrchdRequest::ArchiveProject`), its inverse does not — an archived project is a one-way trip in v1. Additive (new verb + one new `OrchdPersistError::Invariant`-guarded transition), never blocks S3's DoD | S3 spec §14 (explicitly out of scope) | post-S3 | done (S-POLISH O-3, commit 64618d7, shipped `[0.8.0]` — append-only `UnarchiveProject{id}` verb + `Db::unarchive_project` (`archived→active`; unknown id ⇒ `NotFound`; already-active ⇒ `Invariant`, mirroring `ArchiveProject`), `orchd_unarchive_project` command, and UI controls: a collapsed dimmed «Archived (N)» sidebar group + a read-only archived-project banner with an «Un-archive» button. Tests `persistence::domain_tests::unarchive_project_*` + `dispatch_integration::unarchive_*`) |
| BL-54 | P3 | orchd/export | Chunked export: `ExportProject`/`ExportAll` answer a typed `Io` "exceeds 16 MiB frame cap" error instead of a doomed oversized send rather than chunking the reply across multiple frames — correct and honest at v0.x scale (no real store is near 16 MiB), irrelevant until project/store size grows | S3 spec §8 (explicitly deferred) | post-S3, size-driven | open |
| BL-55 | P3 | frontend/tasks | Panel-level cross-project task rank: S3 ships per-project `rank` (`SetTaskRank`, midpoint math) only — Q9's second half, a panel-level rank spanning ALL projects (for a future cross-project kanban board), is not built; owner decision Q9 named it an additive column for later, not a v1 requirement | Platform overview §3 Q9; S3 spec §14 | S5 | open |
| BL-56 | P3 | frontend/ideas | Spawn-project-from-idea UI flow: the data enabler (`Idea.project_id: Option<String>` + `SetIdeaProject`) ships in S3 so an idea CAN be attached to a project, but the UI flow that creates a new Project+Workspace scaffolding directly FROM an idea (S3 spec D3) is not built — deliberately deferred, S-IDEA is the first v2 slice to need it | S3 spec D3/§14 | S-IDEA | done (S-IDEA — `src/components/idea/SpawnProjectFromIdea.tsx`: pure frontend orchestration over the three existing verbs in order, `pickFolder`→`createWorkspace`(sessiond)→`orchdCreateProject{name:idea.title, workspace_ids}`→`orchdSetIdeaProject` — no new orchd verb, per S-IDEA spec D6) |
| BL-57 | P3 | frontend | `RulesetPanel.tsx`'s `ChipList` (`approvalClasses`/`pathAllowlist` policy-form inputs) does not de-dupe on add: `commitAdd()` calls `onAdd(trimmed)` for any non-empty trimmed draft without checking whether `trimmed` already exists in `values` — submitting the same class/path twice adds a duplicate string to the array (harmless server-side, `validatePolicy` doesn't reject duplicates either) but the chip list's `key={v}` then collides on React's side for the two identical chips | Task 17 review (T17 Minor) | post-S3 polish | open |
| BL-58 | P3 | orchd/docs | `crates/orchd/src/socket_server.rs`'s module doc overclaimed it was the ONLY place in the crate calling `SystemTime::now()` — `persistence.rs` has its own `now_ms`/`now_secs` for row `created_at`/`updated_at` and DB-quarantine filenames. Reworded to scope the claim to the `exported_at` stamp specifically | Task 10 review (T10 Minor) | T21 (this change) | done (fixed in the same change that filed this row — `crates/orchd/src/socket_server.rs` doc comment reworded, T21 Step 9) |
| BL-59 | P1 | release/packaging | `bpa-orchd` was NOT bundled into a release `.app`: `bundle.externalBin` listed only `bpa-sessiond`, and `scripts/build-universal.sh` only built/staged/lipo-merged `bpa-sessiond` — so a signed/notarized release built then would have shipped WITHOUT the second daemon (the S3 UI would show `orchd://down` forever). Missed by every gate because `npm run tauri dev` and the e2e harness resolve `target/debug/bpa-orchd` directly, never exercising the bundler path | T21 docs-truth pass (README Quickstart audit) | T21 follow-up | done (T21 follow-up commit — `src-tauri/tauri.conf.json` `externalBin` now `["binaries/bpa-sessiond", "binaries/bpa-orchd"]`; `scripts/build-universal.sh` `build_sidecars()` builds+stages+placeholder-checks bpa-orchd for aarch64+x86_64 exactly like sessiond; `.github/workflows/ci.yml` gates job stages both (Tauri's build.rs now hard-requires both externalBin entries per host triple — verified: adding orchd to externalBin made `cargo build -p builder-pro-ai` fail with "resource path `binaries/bpa-orchd-aarch64-apple-darwin` doesn't exist" until staged, so CI staging is load-bearing, not cosmetic); `.gitignore` ignores `bpa-orchd-*` like `bpa-sessiond-*`; `scripts/sign-verify.sh` now verifies BOTH embedded sidecars' signatures (loop over `bpa-sessiond`/`bpa-orchd`); `scripts/smoke-clean-vm.sh` waits for BOTH daemons to start under launchd on first launch; README Quickstart + architecture note + `docs/build-macos.md` step-1 corrected. **Verified:** `bash -n` clean on build-universal.sh/sign-verify.sh/smoke-clean-vm.sh, `tauri.conf.json` valid JSON with both entries, full 9-stage `final-suite.sh` green with both externalBin entries. **NOT independently verified here** (heavy, credential-gated): an actual universal `lipo` + Developer-ID-signed + notarized build and a clean-VM Gatekeeper smoke — the packaging wiring is correct and gated, but the end-to-end notarized artifact still wants a real signed build on a credentialed machine before a first public release, per `docs/build-macos.md`) |
| BL-60 | P3 | orchd/import | Import raw-insert doesn't enforce same-project-parent / cycle-freedom for goals: `insert_goal_raw` (`crates/orchd/src/persistence.rs`) writes a bundle row's `parent_id` verbatim with no check that the parent belongs to the same project or that the resulting edge set stays acyclic — a hostile export bundle can create an orphaned/invisible goal cycle (never crashes; `list_goals`' recursive CTE just never seeds a cycle with no path back to the strategic root, so the rows exist but no UI ever renders them). Spec §8 explicitly disclaims deeper validation for v1. Add same-project-parent + cycle checks to `insert_goal_raw` if hostile bundles become a threat model | S3 final-review pass | post-S3, threat-model-driven | open |
| BL-61 | P3 | frontend | Locked honest-degradation copy «the project must keep a workspace» (spec §11, `RemoveProjectWorkspace`'s last-workspace guard) is never actually produced: the backend's `OrchdPersistError::Invariant` carries the English message `"cannot remove the project's last workspace link"` (`crates/orchd/src/persistence.rs`), and `describeOrchdError` (`src/ipc/orchd.ts`) wraps whatever string it receives verbatim (`` `invalid operation: ${message}` ``) rather than mapping it to the locked friendly copy. Either localize known `Invariant` message strings in `describeOrchdError`'s switch, or have the backend send a machine-readable code the frontend maps to locked copy | S3 final-review pass | post-S3 polish | open |
| BL-62 | P3 | orchd/tests | Graph no-secrets-in-logs coverage missing: S4 spec §8 called for extending orchd's `no_secrets_in_logs` test to plant a marker in a graph node's `label`/`body` and assert it never reaches the tracing log ("or its graph-covering sibling") — that extension was never made. `crates/orchd/tests/no_secrets_in_logs.rs` still only covers RuleSet markdown content (`planted_ruleset_secrets_never_appear_in_logs`), unchanged since S3. No known live leak (manual review of `graph.rs`/`socket_server.rs`'s graph dispatch arms finds no code path logging a node's `label`/`body` — the one `tracing::error!` in the `GraphAddEdge` handler logs only `edge_id`/`error`), but the discipline is untested for this content-bearing table, unlike every other one | S4 spec §8 (planned, not implemented) | S4 follow-up | done (S4 follow-up commit — the "graph-covering sibling" the spec text anticipated: new `crates/orchd/tests/no_secrets_in_logs_graph.rs::planted_graph_node_secrets_never_appear_in_logs`, a SIBLING file rather than a second `#[test]` in `no_secrets_in_logs.rs` because `bpa_daemon_core::logging::init_to_file` installs a process-global subscriber and refuses a second call in the same process, and `cargo test` runs every `#[test]` in one integration-test FILE in the same process. Plants two distinct markers (one in `label`, one in `body`) on a real on-disk `Db`, drives `create_project` → `add_node` → `update_node` (re-plants the body secret into the label field, a second SQL statement) → `add_edge` → a duplicate `add_edge` (`Conflict` error path) → a self-loop `add_edge` on the same secret-carrying node (`Invariant` error path) → `delete_node` (cascading DELETE), flushes the real tracing sink, and asserts neither marker appears in the log file. RED-proven: a temporary `tracing::error!` leak inserted into the test driver made the assertion fail before being removed, confirming the test actually catches a leak rather than passing vacuously. Required bumping `Db::add_node`/`update_node`/`add_edge`/`delete_node` (`crates/orchd/src/graph.rs`) from `pub(crate)` to `pub` — not a new exposure, since `socket_server.rs`'s dispatch table already exposes the identical behavior over the wire since T10; the bump only let this integration test (a separate compiled crate) drive the Db layer directly, mirroring how `persistence::Db`'s ruleset/project methods are already `pub` for the same reason. `docs/traceability.md`'s S4 "Known gap" note replaced with a normal contract row) |
| BL-63 | P3 | orchd/graph | Auto-populate the knowledge graph from domain events beyond the D6 strategic-goal seed (e.g. auto-create/update an `entityRef` node + edge when a goal/idea/insight/task is created, tagged, or linked) — S4 ships the retrieval API + the human-facing editor only; population today is either manual (the UI) or the one seeded strategic-goal node per project | S4 spec D12/§10 (explicitly out of scope) | SW2 (domain event bus) | open |
| BL-64 | P3 | orchd/graph | Graph node/edge bulk ops (batch create/delete over one wire round-trip) — today every `GraphAddNode`/`GraphAddEdge`/`GraphDeleteNode`/`GraphDeleteEdge` is one row per request; fine at v0.x interactive-editing scale, a bottleneck once something (research-artifact ingestion, an agent populating a subgraph in one pass) needs to write many nodes/edges at once | S4 spec §9 (docs-truth pass ask) | S-EXT | open |
| BL-65 | P3 | orchd/graph | Retrieval ranking / embeddings / semantic search — S4's `GraphSearch` is structural `LIKE`-text only (case-insensitive substring on `label`/`body`, ordered by `updated_at DESC`, capped at 200); no relevance ranking, no vector/embedding similarity. Fine for a human clicking through a small graph; a real blocker once S6 agents need "find the nodes most relevant to X" over a large workspace-wide graph | S4 spec D12/§10 (explicitly out of scope) | S8 | open |
| BL-66 | P3 | orchd/graph | `neighborhood()`/`search_nodes()` (`crates/orchd/src/graph.rs`) return the STORED (possibly stale) `entityRef` label and always `is_orphan: false` — the read-time label-resolution pass (`resolve_node_label`/`resolve_entity_label`) is wired into `list_project_graph` only (S4 spec §5's "the stored label is fine for search" carve-out covers `search_nodes`; `neighborhood` has no such carve-out, it's simply unresolved). When S6's agent retrieval starts consuming `neighborhood()` as its primary read path, add label resolution + orphan flagging there too — batched per `entity_type` (one `SELECT ... WHERE id IN (...)` per entity table, not a `resolve_entity_label` call per node) so the `<100 ms` perf-DoD budget (`neighborhood_depth_3_on_500_node_1000_edge_graph_is_under_100ms_rooted_at_goal_node`) isn't blown by turning the CTE hot path into an N+1 | S4 final-review pass (T21 fixups) | S6 | open |
| BL-67 | P3 | orchd/graph | `list_project_graph`'s (`crates/orchd/src/graph.rs`) external-ghost dedup is O(n²): `external_ids: Vec<String>` plus a linear `.contains()` check per edge endpoint, rather than a `HashSet`. It also resolves every `entityRef` node's label N+1-style — one extra `SELECT title FROM {table} WHERE id = ?1` (`resolve_entity_label`) per entityRef node, on top of the bulk node/edge queries. Both are fine at v0.x per-project graph sizes (dozens-hundreds of nodes) but would show up once a per-project graph reaches thousands of nodes: switch `external_ids` to a `HashSet<String>` and batch label resolution per `entity_type` (one `SELECT ... WHERE id IN (...)` per entity table instead of one query per entityRef node) | S4 final-review pass (T21 fixups) | S-EXT (perf) | open |
| BL-68 | P2 | orchd/security | `stdio_exec` consent fingerprint is TOCTOU: `trust::stdio_exec_fingerprint` (`crates/orchd/src/trust.rs`) hashes the resolved binary's bytes at *authorize* time, but the actual `bpa_mcp::connect` spawn happens moments later — a local attacker who can rewrite the binary at that exact path in the window between authorize and spawn (or between the owner's consent grant and the next spawn) gets the OLD hash's approval for NEW bytes. The bytes-hash already closes the common "owner points the server at a different binary" case (re-prompt on path/bytes change); this residual window needs hashing the fd actually `exec`'d (open-then-hash-then-fexecve on the same fd) or a spawn-then-verify handshake. Accepted-deferred: requires local FS write to the exact path inside a sub-second in-process window; not reachable remotely | T16 adversarial review (accepted ⚠️) | S6b/hardening | open |
| BL-69 | P2 | orchd/security | stdio MCP child's **stderr is inherited to orchd's stderr/log stream unredacted**: `rmcp`'s `TokioChildProcess` defaults `stderr` to `Stdio::inherit()`, so a (third-party, untrusted) stdio MCP server's stderr writes land in orchd's own logs with no capture/redaction — a channel for log injection or for the server to smuggle content into operator logs. `bpa-mcp`'s `build_stdio_transport` (`crates/mcp/src/transport.rs`) does not override stderr. Capture the child's stderr (piped, size-bounded, secret-scrubbed like the no-secrets-in-logs discipline) or redirect to `/dev/null` before shipping stdio servers to real users | T15/T16 review (accepted ⚠️) | S6b/hardening | open |
| BL-70 | P3 | orchd/mcp | No LIVE `tools/list_changed` subscription: `bpa_mcp::client::connect` (`crates/mcp/src/client.rs`) opens every session with the unit `()` `ClientHandler`, whose `on_tool_list_changed` is the trait's no-op default, AND Phase-1's connect-per-call architecture never holds a session open between calls in the first place (`mcp::invoke::call_tool`/`McpConnect` each open a fresh session and tear it down at the end of that one request) — there is no long-lived connection over which a server could ever push an async list_changed notification for this daemon to observe. Accepted-honest for v1 (task T18, spec §3): the tool cache is instead refreshed on every `McpConnect` (`mcp::lifecycle::connect` always re-`list_tools()`s and wholesale-replaces the cache, pushing `McpToolsChanged`), which covers the practical case (a client about to read the tool list again always triggers a fresh read). The residual gap — a server-side tool-list change between two `McpConnect`/`tools/call` attempts is invisible until the next explicit `McpConnect` — needs a persistent-session architecture (a session held open across calls, with a real `on_tool_list_changed` override pushing `McpToolsChanged` live) to close | T18 (S-EXT §3/§8, BL-22 follow-up) | S6b (alongside the agent org / persistent-session work) | open (S-IDEA note: `ResearchPane`'s research pane is v1-scoped to run STATUS only — pending/running/done/failed — never streamed tokens, for the identical reason this row names: `mcp::invoke::call_tool` is request/response over a connect-per-call session, nothing to subscribe to mid-call. A token-streaming research pane needs the SAME persistent-session architecture this row is already tracking — filed here rather than as a duplicate row, S-IDEA spec §10) |
| BL-71 | P3 | mcp/protocol | MCP **sampling** (server→client LLM calls) is deliberately disabled and not advertised in `initialize`'s client capabilities — a server that wants to ask the client's LLM to do something has no way to in v1. Revisit once an in-app LLM-provider layer exists to actually service a sampling request (S6a) | S-EXT spec §1/§2 D3 (Q6, explicit non-goal) | S6a/S6b | open |
| BL-72 | P3 | mcp/protocol | MCP **resources** and **prompts** are not surfaced anywhere in v1 — `bpa-mcp`/orchd only ever calls `tools/list`+`tools/call`; a server's `resources/*`/`prompts/*` capabilities are invisible to the app even if advertised | S-EXT spec §1 (Q6, explicit non-goal) | post-S-EXT, demand-driven | open |
| BL-73 | P3 | orchd/connectors | Only ONE connector adapter ships (`GenericRestAdapter`, `provider="generic-rest"`) — a named social direct-API adapter (X/LinkedIn, etc.) with a provider-specific op set (typed request/response shapes, not raw `get`/`post`) is not built. The `ConnectorAdapter` trait + account layer are proven end-to-end by the generic-rest reference; a real named adapter is additive work behind the same seam | S-EXT spec §7 (explicit follow-up) | S-IDEA/S9a (first real social-connector consumer) | open |
| BL-74 | P1 | mcp/security | Active tool-result prompt-injection **mediation** at an agent boundary is not built — S-EXT sets `is_untrusted=1` on every `mcp_artifact` (from `McpCallTool` AND `ConnectorInvoke`) and stores it quarantined, but nothing REWRITES, filters, or gates on that flag before feeding a result to an agent, because no agent exists yet to feed it to. This is the split-out remainder of BL-22's original title ("tool-result prompt-injection mediation") — the tag+boundary ship now (BL-22 closed), the consuming mediation step is S6b's job | S-EXT spec §1 (explicit non-goal); BL-22 split | S6b | open |
| BL-75 | P3 | orchd/mcp | No bulk MCP-server import: `McpAddServer` registers exactly one server per request — importing a config file (or a `claude_desktop_config.json`-style bundle) listing N servers needs N round-trips today, no batch verb | S-EXT spec §11 (explicit follow-up) | S-EXT follow-up / SW3 | open |
| BL-76 | P3 | orchd/mcp | `ConnectorInvoke` does not push `McpInvocationLogged` — that push's wire shape is `{server_id: String}` (non-optional), but a connector call only has an `account_id`, so `socket_server.rs`'s `ConnectorInvoke` dispatch arm broadcasts `McpArtifactsChanged` only. A live-refreshing invocation-log UI (`InvocationLog.tsx`) therefore never auto-updates on a connector call — the row appears only after the NEXT unrelated refresh/mount. Needs either a wire change (widen `McpInvocationLogged` to accept an account id) or a parallel `ConnectorInvocationLogged`-shaped push | T13a review (accepted, deferred) | S-EXT follow-up | open |
| BL-77 | P2 | orchd/fs | `skills::registry` (`crates/orchd/src/skills/registry.rs`) and the pre-existing `ruleset_files.rs` both read their markdown files with a plain `std::fs::read_to_string` — no stat-before-read size cap, unlike `fs_explorer::read_file_preview`'s established stat-first/1-MiB-capped/`TooLarge`-honest pattern (S2 spec §4.2). A very large or actively-growing SKILL.md/rules file is read to completion (memory + latency) with no honest-degradation path; a hostile or corrupted file (e.g. someone points `md_path` at a huge unrelated file within the allowed root) has no ceiling. Adopt the same stat-before-read cap for both skills and ruleset reads | S-EXT docs-truth pass (T19) | post-S-EXT polish | open |
| BL-78 | P3 | orchd/ui | No policy-delete UI/verb: `InvocationLog.tsx`'s spend/rate policy editor only ever `TrustSetPolicy`s (a cap CAN be reset to `null`/unlimited on the same scope, which is functionally equivalent to clearing it, per T18's own note) — there is no explicit remove-row control and no `TrustDeletePolicy` wire verb, so a configured-then-abandoned scope's `policy` row lingers in `orchd.db` (harmless — `resolve_policy` treats an all-null row as unlimited — but not removable without a raw DB edit) | T18 review (deferred) | post-S-EXT polish | open |
| BL-79 | P3 | orchd/connectors | `PendingOAuth` (the in-flight `ConnectorBeginOAuth` entry in `ConnectorsState`'s in-memory map) has no TTL/expiry sweep — an owner who starts an OAuth flow and never completes it (closes the browser tab, abandons the connector) leaves the entry in the pending map for the daemon's whole lifetime. Not a security issue on its own (the CSRF `state` stays single-use and unguessable), just an unbounded-but-slow memory-growth vector; add a bounded TTL + periodic sweep if it ever matters in practice | T11 (accepted, deferred) | post-S-EXT polish | open |
| BL-80 | P3 | orchd/connectors | `GenericRestAdapter` has no client-pooling across calls: `connectors::adapter::resolve_adapter` calls `GenericRestAdapter::new()` fresh on every `ConnectorInvoke`/`ConnectorListOps`, building a brand-new `reqwest::Client` (and therefore a fresh connection pool, losing keep-alive reuse) each time rather than holding one long-lived client on `ConnectorsState`. Harmless at v0.x call volumes; worth fixing once a real adapter sees sustained traffic | T12 (accepted, deferred) | post-S-EXT polish | open |
| BL-81 | P2 | orchd/security | Project deletion cascades `mcp_server` rows via FK but can't delete their bearer Keychain entries (SQL can't reach Keychain) — orphaned credentials on project delete; add a pre-cascade bearer sweep or a Keychain GC pass. (S-EXT final-review residual.) | S-EXT final-review (`McpDeleteServer` Keychain-orphan fix) | post-S-EXT polish | open |
| BL-82 | P3 | orchd/research | LLM-computed fit-verdict is not built: `Insight.fit_verdict`/`fit_reasoning` are OWNER-set in v1 (S-IDEA spec D4, overriding the roadmap's Q10 default) because S6a (the native LLM provider layer) doesn't exist yet and the S-IDEA DoD requires the loop to work WITHOUT the S6 agent org. The schema field is already shaped for it (`fit`/`no_fit`/`unknown` + free-text reasoning) — once S6a ships, an agent-computed suggestion with one-click owner override (the original Q10 default) can populate the same field, no schema change needed | S-IDEA spec D4/§10 (explicit non-goal) | S6a | open |
| BL-83 | P3 | orchd/research | Automated agent task-decomposition is not built: v1 decomposition is entirely owner-driven — the owner manually creates subtasks via `CreateTask{parent_id}` on an accepted insight's task. An agent that reads a task and proposes a subtask breakdown is S6b's job (needs the agent runtime this slice deliberately ships without) | S-IDEA spec §1/§10 (explicit non-goal) | S6b | open |
| BL-84 | P3 | orchd/research | No first-class ResearchArtifact provenance viewer: `ResearchPane`'s done-run view reuses the S-EXT `mcp_artifact` viewer verbatim (content + the «unverified data» untrusted banner) — there is no research-specific presentation (e.g. highlighting which idea/insight/task chain a given artifact fed, or a dedicated provenance graph view) beyond what the generic artifact viewer already shows | S-IDEA spec §7 (reused, not built) | post-S-IDEA polish | open |
| BL-85 | P3 | orchd/research | No research-run cancel/retry controls: once `ResearchStartRun` spawns the background task, the owner cannot cancel a `running` run (must wait for it to reach `done`/`failed`, or restart the daemon and rely on D11 boot-reconcile) nor retry a `failed` run with the same args in one click (today: form a new `ResearchRunDialog` from scratch). Add `ResearchCancelRun`/`ResearchRetryRun` wire verbs + UI affordances if this becomes a real friction point | S-IDEA spec §7/§10 (v1 scope line) | post-S-IDEA polish | open |
| BL-86 | P3 | orchd/research | Fit-context shows `metric_refs` as owner-declared strings only — no real metric timeseries. `FormInsightDialog`'s fit-context panel renders a goal's `metric_refs` verbatim (no MetricPoint data to back them, since S8 — metrics ingestion — hasn't shipped); once S8 lands, the same panel should pull actual timeseries instead of bare strings | S-IDEA spec §1 (explicit non-goal, "Metrics ingestion is S8's job") | S8 | open |
| BL-87 | P3 | orchd/research | No prowl-aware `session_id` convenience adapter: each `research_run` is one run-scoped tool call, so BPA does not generate or enforce a prowl `session_id` — the owner supplies one via `args_json` like any other tool arg if the picked tool's schema wants one (S-IDEA spec D13, a deliberate v1 override of the roadmap's Q5 default). A convenience adapter that auto-seeds a fresh `session_id` into `args_json` when the picked tool's schema exposes that key would remove the manual step without hardcoding a prowl-specific request shape | S-IDEA spec D13/§10 | post-S-IDEA polish | open |
| BL-88 | P3 | orchd/research | The spawned research-run `JoinHandle` is untracked: `research::start_run`'s `tokio::spawn`'d task is fire-and-forget — `OrchdShutdown{drain}` does not hold or await its `JoinHandle`, so a drain can return before an in-flight run's task has actually stopped running (harmless: D11's boot-reconcile is the authoritative correctness backstop regardless, flipping any non-terminal run to `failed{interrupted}` on the next boot). Tracking the handle so drain could best-effort await it with a short timeout would tighten shutdown latency/tidiness, not correctness — a nice-to-have, not a bug | S-IDEA spec D11 (nice-to-have, explicitly not required for correctness) | post-S-IDEA polish | open |
| BL-89 | P1 | orchd/mcp | **`McpConnect` has no timeout at ANY layer** (UX-audit F-03/B-01, confirmed Critical): the explicit-connect path (`mcp/lifecycle.rs:68-73`) is not `tokio::time::timeout`-wrapped, rmcp's `().serve()` sets none, and `default_http_client()` builds reqwest with no `.timeout()`/`.connect_timeout()` — the S-IDEA D12 fix covered ONLY `invoke::call_tool`'s reconnect path. Worse than a hung dialog: orchd's dispatch is sequential per connection and the app holds ONE shared orchd connection, so a hung MCP endpoint wedges the ENTIRE orchd pipeline (every panel), the client's 30s `REQUEST_TIMEOUT` returns `Disconnected` per-request without surfacing the `orchd://down` banner, and recovery requires restarting orchd. Fix: wrap `connect_fn`+`list_tools` in `lifecycle.rs` with `server.timeout_ms` (mirror invoke.rs) + regression test mirroring `call_tool_connect_that_never_resolves_times_out_not_hangs` | UX-audit `docs/qa/ux-investigation-report.md` F-03 | next fix slice | done (S-POLISH P1, commit 1f28536, shipped `[0.8.0]` — both `mcp::lifecycle::connect`'s `connect_fn` and `list_tools` awaits are now bounded by the per-server `timeout_ms` (`McpError::Timeout` on elapse), so a hung MCP endpoint can no longer wedge orchd's sequential dispatch. Tests `mcp::lifecycle::tests::connect_times_out_when_connect_fn_never_resolves`/`connect_times_out_when_list_tools_never_resolves`) |
| BL-90 | P3 | orchd/export | Import writes ruleset `.md` files BEFORE `tx.commit()` (UX-audit C-07/B-07, confirmed latent bug): in a multi-bundle whole-store import, an early bundle's ruleset file lands on disk via `write_atomic`, then a later bundle's PK collision rolls back the ENTIRE DB transaction — but the already-written file survives, contradicting the "nothing survives a failed import" doc comment. Contained to `app_support` (path fail-closed-validated), self-healing on the next successful import → Minor. Fix: stage file writes and apply after commit (or delete staged files on rollback); extend the collision test to assert no orphan file | UX-audit `docs/qa/ux-investigation-report.md` C-07 | polish | done (S-POLISH P1, commit 4c49642, shipped `[0.8.0]` — ruleset `.md` writes are staged and applied only after `tx.commit()` succeeds, so a rolled-back whole-store import leaves NO orphan file; `app_support`/path validation unchanged. Test `export::tests::import_conflict_leaves_no_orphan_ruleset_file`) |
| BL-91 | P2 | orchd/connectors | OAuth token-exchange/refresh HTTP client has NO `.timeout()` (UX-audit J-05/B-02, latent Critical): `ssrf_guarded_http_client` (`connectors/accounts.rs:491-496`) sets redirect-none but no request/connect timeout — `ConnectorCompleteOAuth`/token-refresh against an unresponsive IdP would wedge orchd's sequential dispatch exactly like BL-89. UNREACHABLE in v1 (OAuth provider registry ships empty, `begin_oauth` fails first), but wakes up the moment the first provider is registered (D14 phase 3). Fix alongside BL-89: add `.timeout(30s)` mirroring `GenericRestAdapter` (`adapter.rs:109-114`) + test | UX-audit `docs/qa/ux-investigation-report.md` J-05 | with BL-89 fix slice | done (S-POLISH P1, commit c234511, shipped `[0.8.0]` — `ssrf_guarded_http_client_with_timeout` adds `.timeout(Duration::from_secs(30))` to the SSRF-guarded OAuth token-exchange/refresh client (matching `GenericRestAdapter`), keeping `redirect::Policy::none()` + the SSRF guard; now reachable since O-5's provider registry (BL-53 sibling) shipped in the same `[0.8.0]`) |
| BL-92 | P2 | core/frontend | Reconnect rehydration incomplete + research runs never self-refresh (UX-audit K-02+F-10/F-11/P-24, Important): `onOrchdUp` (`App.tsx`) refetches only `projects` + the open project's 6 slices — MISSES research-runs, the entire ext surface (servers/tools/artifacts/accounts/skills/invocations/policies), audit, and global-scope ruleset, so an open «Extensions»/Log/ResearchPane stays stale after an orchd bounce until manual tab round-trip. Compounded: research runs have NO polling/manual-refresh and boot-reconcile emits no push, so an interrupted run shows «running» forever on screen while the DB row is `failed{interrupted}`. Fix: broaden `onOrchdUp` to refetch all live-visible slices (or invalidate-on-mount) + add a manual refresh / short poll on non-terminal research runs | UX-audit `docs/qa/ux-investigation-report.md` K-02/F-10/F-11 | next fix slice | done (S-POLISH P3, commit 9ea6cdd, shipped `[0.8.0]` — `onOrchdUp` now refetches the WHOLE live surface (projects; the open project's goals/tasks/ideas/insights/ruleset/graph; the ext slices; research runs for every loaded idea; the global ruleset when opened; the storage status), and `ResearchPane` self-polls `researchListRuns` every 2s while a mounted run is non-terminal — so nothing reconnects stale after an outage and no interrupted run shows «running» forever) |
| BL-93 | P2 | frontend | Silent no-op triad on fire-and-forget mutations (UX-audit P-01/P-02/P-03, Important): «+ New terminal» (`TerminalTabs.tsx:28-47`), close «×» (`:49-52`), «+ Add workspace» (`WorkspaceSidebar.tsx:68-73`) all call `void handler()` with no try/catch — a rejected `createSession`/`killSession`/`createWorkspace` is swallowed with zero toast. Worse (P-02): the close reject ALSO skips `manager.dispose`, leaving a zombie tab + leaked xterm, and `removeSession` is dead code so the tab doesn't vanish even on success. Fix: wrap each in try/catch → `describeCommandError` toast; dispose the terminal + remove the tab on the success path | UX-audit `docs/qa/ux-investigation-report.md` B-05/B-06/B-01 | next fix slice | done (S-POLISH P3, commit 0fdee94, shipped `[0.8.0]` — «+ New terminal» / close «×» / «+ Add workspace» now try/catch → `describeCommandError` toast instead of a silent `void handler()`; a close disposes the xterm instance AND removes the tab in a `finally` (fixing the dead `removeSession` wiring), so a failed close never strands a zombie tab) |
| BL-94 | P2 | orchd/frontend | Silent DB-degradation modes have no UI indication (UX-audit A-08/A-09, Important, honesty): corrupt-db quarantine (`persistence.rs:117-141`, user sees an empty account indistinguishable from fresh install) and disk-unavailable in-memory fallback (`boot.rs:90-110`, app looks working but nothing persists — whole session lost on restart) both signal only in the orchd log (`warn!`/`error!`); the wire protocol carries no mode flag/event/banner. Fix: surface the degraded mode in the Hello/status handshake → a persistent honest banner («data recovered from backup …» / «work is not being saved — disk unavailable») | UX-audit `docs/qa/ux-investigation-report.md` A-08/A-09 | next fix slice | done (S-POLISH P1 backend + P3 banner, commits 48742d2 + 9ea6cdd, shipped `[0.8.0]` — the boot fact is now on the wire: `Db::open_with_outcome` → `boot::open_db_degrading` → `StorageStatus{storage_mode, quarantined_path}` (`persistent`/`recovered_from_corruption`/`in_memory_fallback`), read via the append-only `GetStorageStatus` verb + `orchd_storage_status` command; the P3 `StorageBanner` renders a persistent red-accent `role="alert"` for the two non-persistent modes) |
| BL-95 | P2 | frontend | Partial-failure without compensation/idempotency + no double-submit guard anywhere (UX-audit E-07/P-09 + G-08 + P-19, Important): SpawnProjectFromIdea leaves an orphaned project+workspace if `orchdSetIdeaProject` fails after `orchdCreateProject`; «To backlog» leaves the task created but the idea stuck `researching` if `SetIdeaLifecycle` fails after `CreateTask`, and a retry duplicates the task; root cause is NO in-flight busy-guard on any submit (⌘K, CreateProject, ResearchRunDialog, FormInsightDialog, SpawnProjectFromIdea, «+ task») so a double-click double-fires. Fix: add a `submitting` guard (disable during await) to every mutating submit; make the two multi-step chains idempotent or compensating | UX-audit `docs/qa/ux-investigation-report.md` E-07/G-08 | next fix slice | done (S-POLISH P3, commit 0fdee94, shipped `[0.8.0]` — the shared `useSubmitGuard` hook (a synchronous `useRef` re-entry lock, not the batched `submitting` state) wraps every mutating submit, killing the double-fire; `SpawnProjectFromIdea`/`FormInsightDialog`'s multi-step chains now hold the created ids, name exactly what was created, and RESUME from the failed step on retry — never creating a second project/task. Test `src/hooks/useSubmitGuard.test.ts` + per-dialog double-fire tests) |
| BL-96 | P3 | frontend | orchd upgrade-dialog «Cancel» → unreachable-dialog dead-end (UX-audit A-10, Minor): after cancelling the orchd `UpgradeDialog`, `orchdIncompatible` stays true but `orchdDown` is false, and no banner reads `orchdIncompatible`, so the dialog cannot be reopened until an app restart (sessiond has a symmetric return path via `DaemonBanner`'s «Update»). Fix: render a persistent «orchestrator background service is outdated — update» banner keyed on `orchdIncompatible` that reopens the dialog | UX-audit `docs/qa/ux-investigation-report.md` A-10 | polish | done (S-POLISH P3, commit 9ea6cdd, shipped `[0.8.0]` — a persistent `OrchdUpgradeBanner` keyed on `orchdIncompatible` with an «Update» button reopens the orchd `UpgradeDialog` after a cancel, mirroring sessiond's `DaemonBanner` return path. Test `src/components/OrchdUpgradeBanner.test.tsx`) |
| BL-97 | P3 | frontend | Toast is a single-slot clobbering queue with no manual dismiss (UX-audit K-01/P-21, Minor but pervasive): `showToast` overwrites the current toast instantly, auto-dismiss is 4s only, and `dismissToast` is defined+tested but wired to no control (dead code) — under a burst of errors (inline+toast on the same submit, or a refresh-action toast right after a submit toast) the first message is provably lost. Fix: queue toasts (or stack), add a manual close button (wire the existing `dismissToast`) | UX-audit `docs/qa/ux-investigation-report.md` K-01 | polish | done (S-POLISH P3, commit 9ea6cdd, shipped `[0.8.0]` — `showToast` now APPENDS to a capped-at-5 FIFO queue (drop-oldest) instead of clobbering a single slot; the visible toast is always `toastQueue[0]`, auto-advances after 4s, and the now-wired `dismissToast` close button advances early; the auto-advance timer is re-armed per head so a stale timer never clears a later toast) |
| BL-98 | P3 | frontend/ui | Six modal dialogs (`CreateProjectDialog`, `UpgradeDialog`, `ext/ConnectDialog`, `QuickCapture`, `idea/ResearchRunDialog`, `idea/FormInsightDialog`) re-implement the overlay scrim (`rgba(0,0,0,0.4)`) and the on-accent primary-button style locally instead of reusing the `Dialog`/`Button` primitives from `src/ui/primitives.tsx` (S-UXR whole-branch review, Minor). They were intentionally left bespoke in Part B because the `Dialog` primitive would drop their `aria-labelledby`, primary-button autofocus, and no-overlay-close behavior (and add a ✕) — a DOM/behavior-contract break, especially for the security-relevant `ConnectDialog`. Fix: extend the `Dialog`/`Button` primitives to support these opt-in behaviors (labelled-by id, focus-ref, `dismissOnOverlay=false`, optional close control), then migrate the six dialogs onto the kit so the scrim/on-accent literals live in exactly one place | S-UXR whole-branch review 2026-07-18 | next UI polish slice | open |
| BL-99 | P3 | frontend/ui | Density polish flagged by the S-DESIGN review (2026-07-19): (a) `TasksList` always renders all six status groups + a "No tasks" line each (`TasksList.tsx:461-492`), so a near-empty project shows six "(0)" headers — spec-locked "render even empty", but collapsing/dimming empty groups would cut visual noise; (b) the idea row packs ~10 inline controls behind `flexWrap` (`IdeasList.tsx:203-308`) and wraps to 2–3 busy lines — a secondary-action overflow would calm it. Both are Minor, cosmetic. | S-DESIGN review 2026-07-19 | next UI polish slice | open |
| BL-101 | P1 | tauri/boot | **`AppState` was `manage`d too late — boot commands returned the raw Tauri "state not managed" error.** The happy-path `app.manage(AppState{..})` ran inside the async `bring_up_daemon`, AFTER an up-to-~4s bounded connect; the webview loads concurrently and fires `refreshProjects`/`refreshStorageStatus` from frame one, so every orchd/sessiond command in that window failed with `state not managed for field 'state'`. Surfaced by the S-DIAG diagnostics log on a live v0.9.1 install. | S-DIAG diagnostics bundle 2026-07-19 | this fix slice | done (`[0.9.2]`: `setup()` now pre-creates both daemons' slots + write-lock map, resolves both agents, and `manage`s `AppState` SYNCHRONOUSLY before spawning either bring-up task, which now only populate the shared `Arc`s; a first-frame command extracts managed state and returns an honest `Disconnected` + self-heals on `orchd://up`) |
| BL-102 | P3 | test/isolation | `connect_orchd_with_retry_gives_up_after_bounded_attempts_without_panicking` (`lib.rs:~1390`) asserts a connect to a non-existent orchd returns `Err`, but it derives the socket path the same way the real app does — so on a dev machine with a LIVE `bpa-orchd` running (e.g. after installing a release), the test connects to the real daemon and `result.is_err()` fails. CI (clean env, no daemon) is unaffected. Fix: point the test at a guaranteed-dead socket path (unique temp dir / explicit override), not the shared default. **Twin found 2026-07-23:** `connect_with_retry_clamps_zero_attempts_to_a_real_bounded_budget` (`src-tauri/src/lib.rs:1111`, the sessiond-connect path) fails the same way for the same reason — a live `bpa-sessiond` on `/tmp/bpa-501/d.sock`. Both affected tests live in the Tauri core crate, one per daemon; verified 2026-07-23 by running each with `XDG_RUNTIME_DIR` pointed at an empty dir (`ok. 1 passed`) vs ambient (`FAILED`). Both pass in isolation with `XDG_RUNTIME_DIR` pointed at an empty dir; in a full-suite run they still fail because other tests mutate `XDG_RUNTIME_DIR` process-globally and these two are not in that serialization group — so the fix is an explicit per-test socket override, not just joining the existing HOME/XDG lock. | observed 2026-07-19 while verifying BL-101 | next test-hygiene pass | done (2026-07-24 audit remediation — all three affected tests (`…gives_up…` ×2, `…clamps_zero…`) now take `ENV_TEST_LOCK` and point `XDG_RUNTIME_DIR` at a fresh `tempfile::tempdir()`, i.e. they join the very serialization group this row said they were missing and get a guaranteed-dead socket in one move; verified green on a machine with BOTH live daemons running — 7/7 in the filtered run) |

| BL-100 | P4 | frontend/ui | Token-discipline tail (S-DESIGN review): a handful of hardcoded px that should be `--sp-*` — `App.tsx:611` `gap:6`, `ProjectPanel.tsx:311` `gap:16`, and pill radius `999` in a few lists — should map to `--sp-2`/`--sp-4`/`--r-*`. (The `paddingLeft`/`height:32` literals in `GoalTree`/`TasksList` are deliberately px — indent tests assert exact values — leave those.) Trivial, non-blocking. | S-DESIGN review 2026-07-19 | next UI polish slice | open |
| BL-103 | P2 | tauri/security | `fs_explorer` commands (`list_dir`/`read_file_preview`/`delete_entry`/`rename`/`move`/`open_external`, `src-tauri/src/fs_explorer.rs:478`) trust a fully webview-supplied `root: String` and only containment-check `rel` against it — no allowlist ties `root` to a registered workspace root, so injected script (were an HTML sink to exist) could pass `root="/"` for arbitrary read/trash/open. Defense-in-depth today (no HTML-injection sink — guarded by `smoke.test.ts`; and BL-2/CSP is the execution-vector mitigation). Fix needs the stateless pure-local commands to gain access to the registered-roots set (State injection) → validate `root` is (canonicalized) a member of the active workspace's roots; ship with negative tests (`root="/etc"` rejected). | 2026-07-19 audit §2.7 | fs-command-hardening slice | open |
| BL-104 | P2 | ci/coverage | The 80% line-coverage gate (`scripts/coverage-gate.sh`) covers only `bpa-sessiond` + `bpa-orchd`; the entire Tauri bridge crate (`commands.rs` ~205 KB, `fs_explorer.rs`, `broker.rs`, `orchd_client.rs`, `socket_client.rs`) — the security-critical seam — has no coverage floor. Add a `cargo llvm-cov --package builder-pro-ai` gate (or a workspace floor) so the bridge can't silently rot. | 2026-07-19 audit §2.5 | CI hardening | open |
| BL-105 | P2 | release | `scripts/sign-verify.sh` and `scripts/smoke-clean-vm.sh` are orphaned — not referenced by `ci.yml`, `release.yml`, or `final-suite.sh`; `release.yml` signs+notarizes but never verifies the produced artifact, so a broken entitlement / failed staple ships unverified. Add a "verify signature + notarization" step to `release.yml` running `sign-verify.sh` against the collected `.app`/`.dmg`, failing the job on mismatch. | 2026-07-19 audit §2.7 | next release-pipeline pass | open |
| BL-106 | P2 | e2e/ci | The e2e orchd `phase7` (Keychain connector coverage, `tests/e2e/orchd-survive.mjs`) has SKIP branches that still exit 0, and CI never asserts it actually ran (unlike the BL-16-hardened shell tests) — a Keychain-setup regression silently forfeits all connector coverage. Mirror the BL-16 pattern: assert a "phase7 OK" marker / fail on a SKIP line. (Related local-run note: the Keychain-gated orchd tests block on a macOS access prompt on a dev machine without CI's throwaway-keychain setup — the full `cargo test --workspace` is CI-first for that subset.) | 2026-07-19 audit §2.5 | qa/comprehensive-audit-2026-07-24 | done (2026-07-24 — `tests/e2e/orchd-survive.mjs` tracks a `phase7Ran` flag set only on phase7's full happy-path completion; at the end of `main()` a SKIP fails the run when `BPA_REQUIRE_KEYCHAIN=1` is set, otherwise logs an allowed WARNING. `.github/workflows/ci.yml` sets `BPA_REQUIRE_KEYCHAIN=1` on the e2e orchd step (same job that provisions an unlocked search-list keychain), so a CI SKIP — a keychain-provisioning regression — now fails the gate instead of passing vacuously. Verified: phase7 runs fully on an authorized binary; the gate logic is wired; local runs WARN, not fail.) |
| BL-107 | P2 | test/isolation | The `keychain_available()` skip-guard (`crates/orchd/src/connectors/accounts.rs:818`) is written for a Keychain *error* — it probes with a `set → get → delete` round-trip and skips loudly when any step returns a bad OSStatus. It has no answer for a Keychain *prompt*: on a dev machine whose binary is not authorized for the login keychain, the probe blocks on a GUI authorization dialog that a non-interactive shell never answers, so the guard hangs instead of skipping. Observed as `cargo test --workspace` sitting at 0% CPU for 14+ min inside `connectors::accounts::tests` — indistinguishable from a slow compile, which is what makes it expensive. Fix: wrap the probe in a bounded timeout (spawn the round-trip on a worker thread, `recv_timeout` a few seconds) and treat the timeout as one more loud SKIP reason. Same treatment for the sibling guard in `no_secrets_in_logs_mcp.rs`. | observed 2026-07-20 running final-suite.sh before PR #5 | qa/comprehensive-audit-2026-07-24 | done (2026-07-24 — a shared hang-proof `bpa_secrets::keychain_available(timeout)` runs the set→get→delete round-trip on a worker thread bounded by `recv_timeout`; a timeout is one more loud SKIP reason, never a hang. All SIX call sites delegate to it (`connectors/accounts.rs`, `connectors/adapter.rs`, `tests/no_secrets_in_logs_mcp.rs`, `tests/dispatch_integration.rs` ×2, and `bpa-secrets`'s own cfg(test) probe). The orphaned worker thread (prompt never answered) lives for the rest of the short-lived test process — accepted. Verified: `cargo test --workspace` no longer hangs; `bpa-secrets` round-trip dropped 132 s → 1.67 s; full workspace green. See `docs/qa/2026-07-24-comprehensive-qa-report.md`) |
| BL-108 | P3 | test/flake | **Two load-sensitive flaky tests in `bpa-sessiond`'s lib binary.** `attach::tests::natural_exit_final_output_reaches_attached_client_and_entry_is_reaped` (`attach.rs`) and `socket_server::tests::on_exit_flush_persists_final_scrollback_and_exited_lifecycle` (`socket_server.rs`) both fail intermittently under parallel-test load and pass 3/3 in isolation (`--test-threads=1`). Measured on 2026-07-23: attach ~1-2 per 6-16 full-suite runs on BOTH a clean tree and a changed one (so not a regression — the RemoveWorkspace work was ruled out by measuring both); the socket_server one surfaced only while a second toolchain-heavy process compiled concurrently. Failure mode in attach: the forwarder's channel closes after the shell's echo but before `FINAL_MARKER`, i.e. a real drain/ordering race between child exit and final-output forwarding rather than a slow-machine timeout — the test's own comments already note sporadic failures under load. Fix: make the final-drain deterministic (await the forwarder's completion signal instead of racing it) rather than raising sleeps; the same treatment likely covers the socket_server flush assertion. | observed 2026-07-23 verifying the RemoveWorkspace slice | qa/comprehensive-audit-2026-07-24 | done (2026-07-24 — root cause was a PRODUCTION drain race, not test timing: `AttachRegistry::remove_session` (called from the supervisor's WAIT thread via `on_exited`) called `supervisor.unsubscribe_output(...)`, which yanked the reader's sink BEFORE the reader thread broadcast the session's final chunk — silently truncating trailing output to every attached client. Removed the `unsubscribe_output` call from `remove_session`; the reader's own `sinks.clear()` on EOF/exit is the authoritative, race-free graceful drain (runs strictly AFTER the read loop broadcasts every byte). For KillSession this is moot (`Supervisor::kill` joins the reader). Verified: 167/167 `bpa-sessiond` lib + full workspace green. The `socket_server::on_exit_flush_*` sibling did not repro after the fix. See `docs/qa/2026-07-24-comprehensive-qa-report.md`) |
| BL-109 | P1 | tauri/launchd | **REL-1: every GUI launch killed both daemons (and every live terminal) via `bootstrap()`'s bootout branch.** `launchd.rs` `bootstrap()` treated launchctl exit 5 ("already bootstrapped") as *drift* and ran `bootout` — a SIGTERM to the healthy running daemon — then re-bootstrapped, on EVERY app start. But `launchctl bootstrap` returns exit 5 on an already-loaded label REGARDLESS of whether the plist changed (it does not diff), so exit 5 carries no drift information: the bootout fired on every routine launch, killing `bpa-sessiond` (all live PTYs/agent processes) and `bpa-orchd` (flipping in-flight research runs to `failed{interrupted}`, dropping the pending OAuth map). This silently voided the app's core survival promise ("live shells survive the GUI closing") on the most common action, and bypassed the consent-gated upgrade flow (the old daemon was force-replaced before any handshake — also making BL-34's premise outdated). | `docs/qa/2026-07-24-full-audit-report.md` §3.1 REL-1 (empirical ×3) | qa/comprehensive-audit-2026-07-24 | done (2026-07-24 — `launchd.rs::bootstrap()` now treats exit 0 OR exit 5/`is_already_signal` as SUCCESS with NO bootout ("already loaded" is the goal, and the service may hold live sessions). A plist/binary change that genuinely needs a reload goes through the consent-gated upgrade flow (`kickstart_force`, T10b dialog), not a blind bootout — consistent with BL-34's documented "stale-but-compatible daemon is not force-restarted on the plain boot path". The misnamed pinning test `bootstrap_already_bootstrapped_is_success` (which asserted bootout WAS invoked) is flipped to `…_is_success_without_bootout` asserting exactly one bootstrap call, no bootout. Verified: 12/12 `launchd` tests + full `cargo test --workspace` green. See `docs/qa/2026-07-24-comprehensive-qa-report.md`) |
| BL-110 | P1 | orchd/security | **SEC-1: `McpCallTool` on an HTTP server was not re-gated by connect consent after a server URL change.** Consent was checked only at `McpConnect` time; per-call consent gating in `mcp::invoke::call_tool` covered only stdio. An `McpUpdateServer{url}` after the grant repointed the server, and the next `McpCallTool` (and the research driver) reached the NEW url with the resolved bearer, audited `tool_call/allow` — a bearer-exfil path. | `docs/qa/2026-07-24-full-audit-report.md` §3.3 SEC-1 (empirical) | qa/comprehensive-audit-2026-07-24 | done (2026-07-25 — `call_tool` now gates EVERY call (not just stdio) via `connect_action`/`authorize`, so an HTTP url change after a grant re-prompts (`ConsentRequired`) instead of routing the call to the new url. Test `call_tool_http_server_re_prompts_after_a_url_change`; the now-stale `…http_server_is_unaffected_no_extra_gate` test was flipped to the SEC-1 regression. invoke/research test servers grant connect consent to mirror the production flow.) |
| BL-111 | P1 | orchd/security | **SEC-2: the stdio `stdio_exec` consent fingerprint hashed only the resolved binary's bytes**, so `McpUpdateServer` could rewrite `args` (`["-c","<payload>"]`) or inject `env` (`NODE_OPTIONS`/`PYTHONPATH`/`BASH_ENV`…) after a grant and re-use it → arbitrary code execution under a stale consent. | `docs/qa/2026-07-24-full-audit-report.md` §3.4 SEC-2 (empirical) | qa/comprehensive-audit-2026-07-24 | done (2026-07-25 — `stdio_exec_fingerprint` now covers `command + args + env + sha256(binary)` (BTreeMap for deterministic order); any args/env change re-prompts. Test `stdio_exec_fingerprint_covers_args_and_env_so_a_stale_grant_cannot_arbitrary_exec`.) |
| BL-112 | P2 | orchd/export | **DOM-3: `SetTaskRank(±Infinity)` was accepted**; the next `ExportAll` serialized `rank:null` and the store could not re-import its OWN export (one Infinity rank = an un-backupable store); NaN violated NOT NULL. | `docs/qa/2026-07-24-full-audit-report.md` §4.2 DOM-3 (empirical) | qa/comprehensive-audit-2026-07-24 | done (2026-07-25 — `set_task_rank` rejects non-finite ranks with `Validation`; test `set_task_rank_rejects_non_finite_so_the_store_stays_backupable`.) |
| BL-113 | P2 | sessiond | **SES-4: `CreateSession` did not validate `workspace_id`** — a session against a bogus workspace ran (PTY spawned) but every persist failed on the FK (log-only) and it vanished on the next restart with no client-visible error. | `docs/qa/2026-07-24-full-audit-report.md` §4.1 SES-4 (empirical) | qa/comprehensive-audit-2026-07-24 | done (2026-07-25 — new `Db::workspace_exists`; `CreateSession` rejects an unknown workspace up front as `NoSuchWorkspace` before spawning. Test `workspace_exists_distinguishes_known_from_unknown`.) |
| BL-114 | P2 | sessiond | **SES-6: `RemoveWorkspaceRoot` on the last root was a silent no-op returned as success** — the incoming path wasn't canonicalized, so it never matched the stored canonical root, hiding the `LastRoot` guard. | `docs/qa/2026-07-24-full-audit-report.md` §4.1 SES-6 (empirical) | qa/comprehensive-audit-2026-07-24 | done (2026-07-25 — new `canonicalize_root_for_match` (canonicalize, falling back to canonical-parent+basename for a deleted root); `RemoveWorkspaceRoot` canonicalizes before matching so the `LastRoot` guard fires. Tests for both branches.) |
| BL-115 | P2 | orchd/security | **SEC-6: `McpConnect` silently re-enabled every per-tool allowlist entry** (`upsert_mcp_tools` inserted `enabled=1`), erasing an owner's "disable this dangerous tool" on every reconnect. | `docs/qa/2026-07-24-full-audit-report.md` §4.3 SEC-6 (empirical) | qa/comprehensive-audit-2026-07-24 | done (2026-07-25 — `upsert_mcp_tools` snapshots each existing tool's `enabled` by name and a re-advertised name KEEPS its prior flag; only a brand-new name defaults to enabled. Test `upsert_mcp_tools_preserves_a_disabled_tool_across_reconnect`.) |
| BL-116 | P2 | core/fs | **FS-3: `delete_entry`/`rename_entry`/`move_entry` accepted `rel == ""` (the workspace root itself)** — `delete_entry(root, "")` trashed the ENTIRE workspace (data-loss class; UI-unreachable today but the command layer was unprotected). | `docs/qa/2026-07-24-full-audit-report.md` §4.6 FS-3 (Rust pin-test) | qa/comprehensive-audit-2026-07-24 | done (2026-07-25 — new `rel_is_root_itself` guard rejects the root for all three destructive verbs with `OutsideRoot` (fail closed). Tests `delete_entry_rejects_the_root_itself…` + `rename_and_move_reject_the_root_itself`.) |
| BL-117 | P1 | frontend/graph | **GRAPH-1: cross-project "ghost" graph nodes were draggable/selectable**, so an accidental drag/delete fired `GraphMoveNode`/`GraphDeleteNode` at a foreign id (silently rewriting another project's layout / cascade-deleting its node + edges). | `docs/qa/2026-07-24-full-audit-report.md` §3.2 GRAPH-1 (empirical) | qa/comprehensive-audit-2026-07-24 | done (2026-07-25 — `toFlowNodes` marks external nodes `draggable:false`/`selectable:false` (added to the `FlowNode` interface); `handleDeleteSelected` filters external ids as defense-in-depth. Test in `graphMapping.test.ts`. Daemon-side cross-project scoping is a separate wire follow-up below.) |
| BL-118 | P2 | sessiond | **SES-1: `RemoveWorkspace` is not serialized with `CreateSession`** — a create storm during removal leaves orphan live sessions/PTYs on the "deleted" workspace (49/50 in a probe), vanishing silently at the next daemon restart. Fix: serialize create/remove per workspace (per-workspace mutex or a "closing" flag that fails `CreateSession` immediately). | `docs/qa/2026-07-24-full-audit-report.md` §3.5 SES-1 (empirical) | qa/comprehensive-audit-2026-07-24 | open |
| BL-119 | P2 | frontend | **UX-1: false "empty" flash on every domain list before the first fetch lands** — no store slice has a `loaded` flag (`store.ts` initializes `[]`), so "not fetched yet" is indistinguishable from "genuinely empty"; a user with data sees "The goal tree is empty." on every navigation for the list-latency window (core honesty tenet). Fix: per-slice `…Fetched` flags + a loading row (the pattern already exists in `DocsPanel`). | `docs/qa/2026-07-24-full-audit-report.md` §3.6 UX-1 (empirical) | next UI slice | open |
| BL-120 | P3 | frontend | **FE-6: every toast renders with the danger accent** (`Toast.tsx`) — success notices ("Project created", "Token saved"…) look like errors. Fix: add an optional `tone` to the toast queue (default danger) + mark the success call sites (≈handful of dialog toasts; the toast is fundamentally an error surface per its doc). | `docs/qa/2026-07-24-full-audit-report.md` §5 FE-6 (static) | next UI slice | open |
| BL-121 | P3 | orchd/security | **SEC-2 residual: the env denylist strips only `DYLD_*`/`LD_*`** — language-level interpreter-injection vars (`NODE_OPTIONS`, `PYTHONPATH`, `BASH_ENV`, `PERL5OPT`, `RUBYLIB`…) pass through. BL-111's fingerprint fix closes the stale-grant ACE (an env change now re-prompts), so this is defense-in-depth: a GRANTED env can still inject interpreter code. Extend the denylist — but scope it to the orchd stdio path only (the shared `env_filter` also covers sessiond terminals, where stripping `NODE_OPTIONS` would surprise interactive users). | `docs/qa/2026-07-24-full-audit-report.md` §3.4 SEC-2 | S6b/hardening | open |
| BL-122 | P2 | orchd/graph | **GRAPH-1 daemon-side: graph mutations carry no `project_id` and guard only archived status** — a socket client can move/delete any project's nodes (the UI fix BL-117 closes the reachable path; server-side scoping is defense-in-depth). Append-only: reject cross-project mutations (new verb or an ownership precheck). | `docs/qa/2026-07-24-full-audit-report.md` §3.2 GRAPH-1 | SW2/wire | done (BL-143 merged: scoped graph mutations are typed NotFound, wire [1,1]) |
| BL-123 | P2 | core/fs | fs commands never validate `root` against the registered workspace roots — a webview-supplied `root` (even `/`) passes `validate_path_within`, giving arbitrary FS read/write within process rights (2026-07-24 audit FS-5/ARCH-8; pin-tests in `crates/paths`). Fix: keep a workspace-root snapshot in core State (invalidated on workspace events) and validate per fs command | 2026-07-24 audit | next core-hardening pass | done (P1-hardening 2026-07-26 — `WorkspaceRootsCache` in `AppState` (id-keyed canonical roots, seeded `NeverLoaded`), refreshed wholesale by `list_workspaces` and kept current by workspace pushes + create/add-root/remove commands; `commands::ensure_registered_root` guards every `fs_explorer` command (mapped to `FsError::OutsideRoot`/`NotFound`/`Disconnected`, the latter an additive variant) and `start_workspace_watch` (per-root `WatchError`, fail-closed). `bpa_paths::validate_path_within` stays lexical — the `pin_sus5_*` comment now records that enforcement lives at the command layer. 7 guard tests; 305 lib + 76 fs_* green) |
| BL-124 | P2 | orchd/storage | Degraded-mode gating is partial: only `ImportBundle` refuses in `InMemoryFallback`/`RecoveredFromCorruption` (fixed this sweep); every other mutation is still accepted and silently lost on restart, and rules/doc files still land on disk as phantoms without DB rows. Extend the refusal (or an explicit owner-confirm) to all mutations | 2026-07-24 audit DOM-10 | next storage pass | done (P1-hardening 2026-07-26 — centralized read-only/mutation gate in dispatch_inner: every mutation is refused in degraded storage with a typed Invariant naming the mode; reads/export/status/shutdown stay live; 2 modes × 22 mutation families tested) |
| BL-125 | P2 | orchd/trust | No consent-REVOKE verb on the wire — grants can be overwritten but never deleted by the owner (the SEC-1 fix made mutation invalidate grants server-side; an explicit owner-facing revoke still doesn't exist) | 2026-07-24 audit SEC-1 follow-up | next trust pass | done (P1-hardening 2026-07-26 — `TrustRevokeConsent` tail-appended (wire [1,1]); idempotent Ack, consent_revoke audit row, McpServersChanged push; grant→revoke→call→ConsentRequired tested) |
| BL-126 | P3 | core/fs | `build_watch_error_events` (`src-tauri/src/fs_watcher.rs`) still routes error events by first-match root, not longest-prefix (`build_changed_events` was fixed to longest-prefix in the same wave) | 2026-07-24 audit follow-up | next fs pass | open |
| BL-127 | P2 | daemon | kill -9 on sessiond loses up to ~1 s of scrollback that attached clients already saw (1 s flush tick, no crash checkpoint — verified 3/3). Documented tradeoff; the graceful-drain path already covers SIGTERM — only the SIGKILL window remains | 2026-07-24 audit SES-2 | next sessiond cycle | open |
| BL-128 | P2 | core/upgrade | BL-34 re-evaluation after the REL-1 fix: with `bootstrap()` no longer killing the running daemon on every launch, the IncompatibleDaemon consent/upgrade path is reachable again, and BL-34's stale-binary build-comparison applies as designed. Verify the upgrade flow end-to-end on a real stale daemon | 2026-07-24 audit REL-1 follow-up | next daemon-upgrade cycle | open |
| BL-129 | P2 | orchd/trust | No upper clamp on `timeout_ms`/`max_retries` (`i64::MAX` retries ≈ eternal dispatch task); both arrive over the wire | 2026-07-24 audit SEC-8 | next trust pass | open |
| BL-130 | P2 | orchd/connectors | Keychain-write before DB-insert can orphan a secret when the insert fails (no cleanup on the error path — `complete_oauth`/`add_apikey`) | 2026-07-24 audit SEC-9 | next connectors pass | open |
| BL-131 | P3 | orchd/connectors | Token-refresh race: two parallel invokes with an expired access token both exchange the refresh token (no per-account mutex; rotation at the IdP breaks one) | 2026-07-24 audit SEC-10 | next connectors pass | open |
| BL-132 | P3 | orchd/mcp | No size cap on a `tools/call` result (connector bodies are capped at 8 MiB; MCP results aren't) — rmcp-internal limits unverified | 2026-07-24 audit SEC-11 | next MCP pass | open |
| BL-133 | P2 | orchd/mcp | Owner decision needed: generic-REST connector sends the account bearer to an arbitrary per-call `args["url"]` with default redirect following (code comment calls it intended) — sign off explicitly or add a per-account host allowlist | 2026-07-24 audit SEC-7 | owner | open |
| BL-134 | P2 | orchd/wire | MCP artifact >16 MiB: `insert_artifact` has no cap, and `McpGetArtifact`'s oversize frame fails encode → the daemon drops the whole client connection without an error response (static finding; not dynamically probed) | 2026-07-24 audit | next wire pass | done (P1-hardening 2026-07-26 — schema v8 `mcp_artifact.truncated`; insert caps content_json+text at 15 MiB total on a UTF-8 boundary; flag surfaced on McpArtifact/McpCallResult — oversize frames now encode instead of dropping the connection) |
| BL-135 | P2 | core/orchd | No pull-based orchd status command (sessiond has `daemon_status`; orchd relies on push only) — a lost `orchd://incompatible` event makes the upgrade UI unreachable (deferred in-code at `commands.rs:88-90`) | 2026-07-24 audit REL-2 | next core pass | open |
| BL-136 | P3 | core/clients | `request()` TOCTOU: a verb checked in before a drop can be written into the NEW connection after reconnect — undefined semantics for non-idempotent `Create*` | 2026-07-24 audit REL-6 | next client pass | open |
| BL-137 | P3 | core/boot | Worst-case boot-connect ≈44 s (8 × (0.5 s + 5 s preamble timeout)) vs documented "~4 s" when the daemon accepts but never handshakes | 2026-07-24 audit REL-8 | docs/next boot pass | open |
| BL-138 | P2 | daemon/perf | 61× `.lock().unwrap()` in `pty_supervisor.rs` (plus client/command sites) — one panicking thread poisons the whole PTY supervisor (and a panicking flusher kills daemon-wide persistence); unify on the `into_inner()` recovery pattern already used in `socket_server.rs` | 2026-07-24 audit ARCH-1 | next sessiond cycle | done (P1-hardening 2026-07-26 — unified `bpa_protocol::sync::{lock,read,write}` (lives in protocol, NOT daemon-core, because of the locked "src-tauri gains NO bpa-daemon-core dependency" contract); applied to every production `std::sync` lock in `pty_supervisor.rs`/`socket_server.rs`/`attach.rs`, daemon-core `broadcast.rs`/`logging.rs`, and src-tauri `socket_client.rs`/`orchd_client.rs`/`commands.rs`/`lib.rs` (test-only unwraps untouched). Poison-recovery unit tests + a flusher-survives-poisoned-sessions-map test + a poisoned-`live_sessions` ListSessions test in sessiond; 175 sessiond + 305 lib green) |
| BL-139 | P2 | core/clients | `socket_client.rs` vs `orchd_client.rs` drift has begun: per-request completion trace exists only in the orchd one; the 7 reconnect-escalation tests exist only in the sessiond one. Extract a generic frame (the CBOR decoder is already generic) or port the tests | 2026-07-24 audit ARCH-2 | next client pass | done (P1-hardening 2026-07-26 — port, not generic extraction (per plan): the per-request completion `info!` trace (verb/outcome/error_code/elapsed) ported into `socket_client::request` (split into `request`/`request_inner`, mirroring the orchd side), and the 6 missing reconnect/handshake tests ported to `orchd_client` (reconnect-gap prompt failure, late `on_conn` replay, transient below/above `HANDSHAKE_SUSPECT_CAP`, genuine `Incompatible` on reconnect, XDG-empty socket-path fallback; the 7th, `reconnects_after_drop_and_delivers_push`, already existed). 20 socket_client + 19 orchd_client tests green) |
| BL-140 | P3 | core/clients | orchd error codes cross three layers as `format!("{code:?}")` strings the frontend matches, while ts-rs exports the same enum camelCase — pin the contract with serde or a test | 2026-07-24 audit ARCH-3 | next client pass | open |
| BL-141 | P3 | core/clients | 4× `unreachable!()` in reconnect paths — safe today by construction, one new `HandshakeError` variant away from a silently dead client; type-narrow `RetryDecision::Fatal` | 2026-07-24 audit ARCH-5 | next client pass | open |
| BL-142 | P3 | deps | Two reqwest (0.12 via `oauth2`, 0.13 elsewhere — documented); `thiserror` 1 vs transitive 2; `rustix` 0.38 vs 1.1 — candidate bumps | 2026-07-24 audit ARCH-6 | any | open |
| BL-143 | P3 | daemon | Backpressure overflow policy undetermined: disconnect-on-outq-full did not reproduce in a 25 s flood window — document the actual behavior and adjust the invariant text | 2026-07-24 audit SES-7 | next sessiond cycle | open |
| BL-144 | P3 | daemon | ~1-line duplicate at the Replay/live seam when attaching under load (subscribe-before-snapshot window) | 2026-07-24 audit SES-8 | next sessiond cycle | open |
| BL-145 | P3 | daemon | `atPrompt` StateChanged pushed ×3 after each command — dedupe consecutive identical lifecycle pushes | 2026-07-24 audit SES-9 | next sessiond cycle | open |
| BL-146 | P3 | daemon | `kill()` blocks a tokio worker up to ~2 s per session (no `spawn_blocking`); `live_sessions` set grows unboundedly; startup emits a `finished` without `started` (zsh precmd) | 2026-07-24 audit SES-11 | next sessiond cycle | open |
| BL-147 | P3 | frontend/a11y | No arrow-key navigation / roving tabindex in any tablist/tree (TerminalTabs, ProjectPanel, ExtPanel, FileTree) — WAI-ARIA tabs/treeview not met | 2026-07-24 audit UX-5 | next a11y pass | open |
| BL-148 | P3 | frontend | No terminal search (⌘F over scrollback) | 2026-07-24 audit UX-6 | next terminal pass | open |
| BL-149 | P3 | frontend | ⌘K quick-capture is undiscoverable — no visible hint anywhere | 2026-07-24 audit UX-7 | next UX pass | open |
| BL-150 | P3 | frontend/a11y | Dialogs (QuickCapture/CreateProject/Upgrade) lack a focus trap — Tab escapes into the background | 2026-07-24 audit UX-8 | next a11y pass | open |
| BL-151 | P3 | frontend | `ResearchPane` polls every 2 s per mounted pane (N panes = N intervals) — consolidate into one poller | 2026-07-24 audit | next frontend pass | open |
| BL-152 | P3 | frontend | Boot hydrate retries forever with no escalation — after N failures the banner should offer next steps (runbook link / open Diagnostics) | 2026-07-24 audit | next frontend pass | open |
| BL-153 | P3 | frontend | Dead IPC wrappers remain (never called from UI): `detachSession`, `getSessionState`, `moveEntry`, `orchdUpdateProject/Task/Insight`, `orchdExportAll/ImportBundle`, `researchGetRun`, `mcpUpdateServer`, `orchdDeleteInsight`, `powerStatus` — wire them to UI or delete (deps/capabilities half was pruned this sweep) | 2026-07-24 audit FE-9 | next frontend pass | open |
| BL-154 | P3 | frontend | WebGL never enabled when `open()` raced a zero-size container (stays on DOM renderer until hide/open cycle); backpressure watermark is a bare `console.warn` invisible to the owner | 2026-07-24 audit FE-10 | next terminal pass | open |
| BL-155 | P3 | orchd/graph | GRP-2 cluster: an idea stays `researching` forever after a failed/interrupted run (no auto-revert); server-side entityRef rename is silently overridden by the read-time resolver (contract lie); the fit-context graph panel in FormInsightDialog is dead by construction (ideas never get entityRefs) | 2026-07-24 audit GRP-2 | next graph pass | open |
| BL-156 | P3 | orchd/graph | A cross-edge with an archived endpoint can't be removed from the live side except by deleting one's own node — UI should explain the Invariant and offer the escape | 2026-07-24 audit GRP-3 | next graph pass | open |
| BL-157 | P2 | orchd/graph | Graph verbs carry no `project_id` — no server-side ownership scoping (UI hardened this sweep: ghosts are read-only client-side; the wire-level scoping is an append-only verb addition when multi-client matters) | 2026-07-24 audit GRAPH-1 | S6b | done (audit remediation — `project_id: Option<String>` (serde-default, wire `[1,1]` unchanged) added to `GraphUpdateNode`/`GraphMoveNode`/`GraphDeleteNode`; a mismatched scope is typed `NotFound` without revealing the foreign node, `None` keeps legacy behavior; the canvas passes its current project on every mutation; dispatch test proves foreign-scope moves are refused with zero pushes) |
| BL-158 | P3 | core/stats | Stats: cancel is frontend-only (the blocking scan runs on); concurrent scans aren't deduped; attribution falls to "other" on symlink/case path differences and is list-order-dependent on shared roots | 2026-07-24 audit STATS-1 | next stats pass | open |
| BL-159 | P3 | frontend | 12 native `window.confirm` calls for destructive actions (extends BL-47 beyond FileTree) — move to the themed Dialog atom | 2026-07-24 audit | next design pass | open |
| BL-160 | P3 | terminal | Restored-session respawn affordance: this sweep added the no-shell hint + dot state; a one-click "restart shell in the same cwd" is not built | 2026-07-24 audit FE-7 follow-up | next terminal pass | open |
| BL-161 | P3 | docs/process | Measured test totals drift within days of measuring (README/traceability went stale between 0.10.0 and its own remediation) — add a refresh-on-ship step or a tiny measure-and-rewrite script | 2026-07-24 audit DOC-2 follow-up | next docs pass | open |
| BL-162 | P3 | core/fs | FS-9 cluster: symlink→directory renders as a file (unexpandable, confusing toast); create-on-existing-name surfaces generic `io` instead of typed `alreadyExists`; FileTree root rows have no path tooltip (same-basename roots indistinguishable); `dropWorkspace` doesn't prune `expanded`/`treeCache`/`selectedFile`; FilePreview isn't refreshed on `fs://changed` | 2026-07-24 audit FS-9 | next fs pass | open |
| BL-163 | P3 | frontend | User-facing strings hardcoded in bypass of `strings.ts`: ErrorBoundary card (4 strings), App.tsx workspace placeholders + 4 of 5 stat-chip labels; one dead key (`strings.common.add`) | 2026-07-24 audit FE-7 | next frontend pass | open |
| BL-164 | P3 | maintainability | Monolith splits when they start hurting: `crates/orchd/src/persistence.rs` (8159 lines, ~75 pub methods, tests interleaved), `src-tauri/src/commands.rs` (5692; the 50 `expect_*` wrappers macro-able), `crates/sessiond/src/socket_server.rs` (4922, mostly tests) — all mechanical | 2026-07-24 audit ARCH-4 | any | open |
| BL-165 | P3 | daemon | Boot-time `panic!`s (tracing init both daemons, `boot.rs` no-usable-DB, `lib.rs` expect on run) bypass the honest `ExitCode` path — return `ExitCode::FAILURE` with the reason instead | 2026-07-24 audit ARCH-7 | next daemon pass | open |
| BL-166 | P2 | test-infra | No frontend coverage measurement or gate at all (the ≥80% gate covers only the two daemon crates; `@vitest/coverage-*` not installed) | 2026-07-24 audit §10 | next CI pass | open |
| BL-167 | P3 | test-infra | No ESLint/Prettier — frontend lint is `tsc --noEmit` only (strict + noUnusedLocals covers part of it) | 2026-07-24 audit §10 | next CI pass | open |
| BL-168 | P2 | test-infra | GUI-level e2e is manual-only (full-GUI confirmation + `smoke-clean-vm.sh` are runbooks; no tauri-driver/webdriver automation) | 2026-07-24 audit §10 | next e2e pass | open |
| BL-169 | P2 | test-infra | Keychain deadlock environment note: a wedged `coreautha` (LocalAuthentication agent) deadlocks the parallel SecItem-based tests inside Apple's Security framework at 0% CPU for hours (observed 2026-07-24 on an 11-day-wedged agent; diagnosed via `sample` → `SecItemDelete → __psynch_mutexwait`). Remedy: `kill -9 <coreautha pid>` and re-run; if it recurs without a wedged agent, run the keychain suites `--test-threads=1`. Worth a runbook line wherever the keychain skip-guards are documented | 2026-07-24 audit §10.2 | docs | open |
| BL-170 | P3 | terminal | Terminal file links still don't support non-ASCII/space-containing paths (the wrong-target variant was fixed this sweep — a trimmed token no longer links to its root — but such paths get no link at all) | 2026-07-24 audit LNK-1 follow-up | next terminal pass | open |
| BL-171 | P3 | orchd/wire | Residual of BL-134: `McpListArtifacts` can still exceed the 16 MiB frame cap when several capped artifacts (~15 MiB each) are listed together — currently bounded only by the client-side `limit`. Proper fix: response budgeting or a list projection without full content | P1-hardening review 2026-07-26 | next wire pass | open |
